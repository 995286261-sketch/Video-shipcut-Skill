# G3 帧率修订记录 v0.1（unicorn-gundam-transition-001）

- 触发：G5 交付包建包前全解码探测发现 final-video 实为 24fps，与批准卡包装决定「30fps」及 30fps 源片不符（问题清单⑧）。用户 09-24 拍板：修 fps，走一遍修订链。
- 根因链：渲染器只读 `editPlan.fps` 机器字段（002-⑨ 继承机制）→ 本轮重建的计划无此字段 → 清单 `targetFps:null` → g4_render/g4_assemble 静默回退硬编码默认 24（装配记录 fpsSource=default-24）；卡上「30fps」仅存在于人话字符串，无机器对应物；源片帧率无任何环节读取。
- 修订：计划补 `editPlan.fps: 30`（把已批准卡的口径落成机器字段，**批准内容不变**）；联动重跑：预览 v0.4、指令 v0.3、分段 30fps 重裁、候选 v0.3、验证 v0.3、卡 v0.5/G4 卡 v0.3。
- 通道：reopen-g3（第三轮），G3/G4 需重新人工批准。
- 测后修补案（待用户拍板，见台账⑧）：甲=validate_g3_plan 强制卡声明 fps 落成计划字段；乙=渲染器对 targetFps 缺失报错禁默认；丙=G5 机器比对 export-config 声明 fps vs 实探 fps。
