# 转场实跑测试 问题清单 v0.1（unicorn-gundam-transition-001，2026-09-24）

> 纪律：测中只记录不动刀，测后统一处理。编号沿用圈外记法①②③。

| # | 节点 | 现象 | 绕行 | 级别 |
|---|---|---|---|---|
| ① | G1 | approve 要求 approvalRef ∈ 收据 basisRefs，但确认记录/审核决定天然是关单时才写的文件；第一次 record-review 时无法预知最终逐字回复。现行做法=确认后补写文件→改收据→重新 record-review→approve（G1 实走一遍才暴露） | 是（补登记） | 中·合同/流程未写明该时序，建议 contract 明文"确认产物可在关单时补入 basisRefs 并重新登记"或脚本自动生成 approval 记录 |
| ② | G2 | G2 approve 的三个引用（approvedNarrationRef/factCitationRef/voiceBriefRef）只认 CLI flag，且仅到 approve 那一步才校验；record-review 阶段不提示 | 是（补 flag） | 低·建议 --help 或合同示例展示完整 flag 组合 |
| ③ | G0/G1/G2 | 克隆项目时整目录拷贝，旧 ID 内嵌文件混入新项目正式产物区，需逐个辨认、归档（G1 删 2 件、G2 归档 8 件、G3 预计全部换发）；无"克隆即洗 ID"工具或清单 | 手工 | 中·若克隆沿用成为常规场景，考虑 material_pack 级别的 clone 辅助；单次场景不建工具（earned vs imagined） |
| ④ | G2 | G2 卡留痕段误写「13 段/12 切点」（引用了旧项目长版规模），实际执行短版 8 段/7 内部切点 | G3 卡更正节如实呈报 | 中·AI 前瞻数字也是幻觉面；卡内引用规模数字应来自盘上计划文件而非记忆 |
| ⑤ | G3 | **编排手制 G3 回显卡，未走 render_g3_review_card.py 固定八列+包装决定节模板，被用户当场识破**（合同明文：八列表+成片包装决定固定节+经验库查询二选一必显）；作废稿留档 | **已闭环（09-24 用户令"模板统一更新"当场落刀，先于"测后统一处理"纪律执行）**：review_gate.py 登记时机械校验渲染器出处标记（手制卡 record-review 直接拒），负向测试+回归 40 绿；g3-choice-cards §4 / pipeline-state-contract / final-echo-style-guide 三处合同同步；本跑重出 v0.3 合规卡并重登记（19 指纹，标记校验活证通过） | 高·「回显必须是固定表格卡片」在 G3 的具体化=必须走渲染脚本；教训=机械校验缺失时口头纪律拦不住编排自己 |
| ⑥ | G3 | 渲染卡 BGM 乐句列档位名重复（"P2 · 常规档 · 常规档"）：bgmPhrase 自带档名 + 渲染器又追加 layoutTier | **改判：非渲染器 bug，是回显数据误用**（bgmPhrase 只应填乐句 ID，档名由渲染器统一追加）；v0.3 数据已修正，渲染输出复核正常 | 低·字段口径以卡样例为准，无需改码 |
| ⑦ | G5 | **字幕复检抓到批准产物真缺陷**：G3 批准的 `G3-字幕时间轴-v0.1.srt`（照搬旧项目）cue1 结束 5372ms 与 cue2 开始 5322ms 重叠 50ms，`subtitle_check_srt.py` 判 failed；G4 烧录 ASS 同源继承（两行同位撞 1.5 帧）。旧项目 G5 早于专员复检纪律，属旧产物不过当前校验（R3 同族）。修复=按 ㉖ 纪律走 reopen-g3 修订链（SRT v0.2→ASS v0.2→候选 v0.2→G3/G4 重批），不得带 failed 握手封包 | 是（修订链进行中） | **根因（用户 09-24 定性=流程设计缺失，复盘四条）**：①G3 登记对 subtitle_timeline 只查文件存在+哈希、从不跑内容校验（R2 防篡改不防带病批准）；②`subtitle_check_srt.py` 只接线在 G5 消费侧——批准它的门禁不看它合不合格，看它的环节在批准之后；③克隆洗 ID（③）无「继承产物按现行校验重跑」项，旧项目带病灶原样入册；④同族=R3（旧产物不过当前校验），三案仍挂。**修补案（测后待用户拍板）：甲=G3 加硬握手（subtitle-srt-check.json 对该轴现算哈希+passed，缺/过期拒登，复刻转场预览门禁模式）；乙=克隆清单加旧产物重检项；甲乙不冲突** |
| ⑧ | G5 | **交付片实际 24fps，与批准的 G3 卡包装决定「30fps」及 30fps 源片不符**：本轮从零重建的计划 JSON 漏写机器字段 `targetFps` → 清单 `targetFps:null` → g4_render/g4_assemble 按合同默认静默降到 24fps（30→24 非整除，轻微抽帧退化）；旧项目成片为 30fps，属本轮引入的回归。G5 全解码探测时抓到 | 是（需 reopen-g3 补 targetFps=30 重渲 G4） | **根因=代码即规格被破坏**：人读的卡说 30、机读的计划没编码 30、缺省值填了个不同的 24，且无门禁拦「卡称X vs 渲染Y」。**修补案（测后待拍板）：甲=validate_g3_plan 强制 packagingDecisions 声明的 fps 落成计划顶层 targetFps 机器字段；乙=g4_render/assemble 遇 targetFps 缺失不再静默默认 24，改为报错要求显式 fps（默认值本身是幻觉面）；丙=G5 验证比对 export-config 声明 fps 与实探 fps（本跑正是靠人工发现，应机器化）** |
| ⑨ | G3/G4 | **fps 机器字段无单一事实源（第三轮修⑧时实锤）**：`transition_preview.py:390` 读顶层 `plan["fps"]`，`g4_prepare.py:169` 读 `plan["editPlan"]["fps"]`——同一概念两个位置，补了 editPlan.fps 预览器仍报「计划无 fps 字段」；任一侧单独填字段=「一半链路知道、一半链路猜/默认」，⑧的孪生缺陷 | 两处都补 fps:30 通过 | 中·合同须钉死 fps 唯一字段位置（建议顶层 `fps`），validate_g3_plan 校验两读取点一致；与⑧甲合并处理 |
| ⑩ | G5 | **修订轮之后 BGM 链路审计 planFileHash failed（15/16 链路其余全过）**：BGM 混音合同 `evidence.planSha256` 锁的是首轮计划文件字节（DB6426CB…），第三轮 fps 修订+批准盖戳改写计划文件（现 c251e92e…），修订链（reopen-g3）与下游合同指纹锁之间没有"合同重锁"协议——混音参数与改动字段完全无关（offset/bed/duck×7/乐句对齐/淡入淡出逐字段一致，BGM 在位实测 −23.3 LUFS 与预测相符） | 如实写入 QA 报告 bgmChainAudit=failed+警告清单，G5 卡人工裁决；不带刀不改合同 | 中·设计缺口：跨修订轮的产物握手合同缺"修订后自洽重建"通道（候选方案：修订合同 amendment 或合同再生成工具，测后与⑦⑧⑨并批处理） |
