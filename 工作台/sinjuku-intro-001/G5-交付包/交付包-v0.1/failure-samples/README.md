# 失败样本与处置（真实发生，非虚构）

1. **㉒ xfade 时基拒绝**（G4 v0.1 首跑）：叠化在 27 段长链中段时 ffmpeg 7.0 报 "First input link main timebase do not match"，成片零输出。处置：`g4_assemble.py` junction 两侧统一 `settb=AVTB`，`g4_validate.py` 期望段长含转场手柄。批测补例待测后批次。
2. **㉖ 审批双义 → reopen 重做**（G3 v0.4→v0.6）：终审卡默认推荐 3 处叠化、关单口令却按"现状 1 处"记录，用户按默认直觉确认后成片少两处叠化。处置：reopen-g3（连带 ㉗ 状态机补 G5 回退路径）→ v0.6 重批 → G4 重跑 v0.2。v0.1 候选/收据/批准记录全部留盘审计。
3. **㉔ 叠化映射观感缺陷**（G4 v0.2 回放）：合同把"叠化"映射到 xfade `dissolve`（噪声抖动式，中点颗粒），非行业语义的平滑叠化。处置：用户 确认G4 接受默认改判 → 映射改 xfade `fade` → 指令 v0.2 → 成片 v0.3，三处中点目视=平滑双影。合同/脚本/能力探测/四套测试同步。
4. **㉓ 描述字段事实错**（成片帧目视逮住）：27 段水印名与封面 note 写错。处置：v0.6 勘误随重批关误，片尾帧复核一致；描述字段机器对账列入测后合同回填。

无静默降级、无虚构通过项。
