# 验收 003 · sinjuku-intro-001 问题清单

> 本跑目的：真实剪辑验收**转场专员**（新上线）与**字幕专员**（首次全链实测），顺带复验 G0–G5 主链。
> 纪律：随测随记，一条一编号；测后由用户统一裁决处理（照 N8/002 先例）。严重度：🔴 高危（静默错误/越权） 🟡 中（体验/歧义） 🟢 低（瑕疵）。

| # | 节点 | 现象 | 期望 | 严重度 | 状态 |
|---|---|---|---|---|---|
| ① | G1 | Freesound 检索通道首跑：4 词检索+预览下载跑了 20+ 分钟仍未结束；脚本 stdout 被管道缓冲，全程零进度行，编排侧只能靠数 candidates 文件判断存活 | 长耗时检索应逐首打印进度（flush）；等待期应可预估 | 🟡 | ✅ 已修（下载逐首 stderr 进度，stdout JSON 合同不动；限速属外因） |
| ② | G1 | 宿主 Bash 默认 python3=3.9 跑 music_analyze → `missing_analysis_runtime`，但真正原因是解释器 ABI 不匹配（库在 3.12 venv），换 venv python 后直接可跑；阻塞文案只教设环境变量、不教选解释器，误导排障方向 | 诊断应区分"环境变量未设"与"解释器不匹配"（打印当前 sys.version 与期望），或合同里明确"用 runtime venv 的 python 调用" | 🟡 | ✅ 已修（区分 env 未设/ABI 不匹配，附 venv 解释器路径；修复中自身踩到 py3.9 f-string 兼容线，被单文件跑法当场逮住） |
| ③ | G1 | 编排侧（AI）自查违规：grep ~/.zshrc 查 runtime 变量时把 `MUSIC_EXPERT_FREESOUND_TOKEN` 整行明文带进了工具输出（本意只查存在性，redaction 正则漏了大写 TOKEN）。token 未进任何仓库文件/回显；用户应视需要轮换 token | 检查机密存在性一律 `${VAR:+yes}`，禁止 grep 可能含机密的文件整行输出 | 🔴 | 已裁决（2026-09-22 用户知情后决定不轮换：泄漏面仅会话日志、token 无资金/隐私权限；纪律教训保留，检查机密存在性仍只准 ${VAR:+yes}） |
| ④ | G1 | 检索词卡再次对参考曲裸脑补体裁：词卡按"史诗/管弦/预告"方向出词，omni A/B 十连听证明《冷战奇迹》参照曲实际是快节奏强鼓点工业电子——62 首检索+62 份分析+10 次 A/B 全部喂给方向错的池子，四首过线曲全部 1–4/10。N8 问题⑫（"一句管弦葬送一轮检索"）原样复发 | 出检索词前必须先有体裁证据：对参考曲跑一次 `music_listen_omni --absolute`（或 A/B 前先听参照曲）拿耳朵级风格事实，词卡的每条"依据"必须引用该证据或用户原话，禁止引用模型没听过的曲目的想象 | 🔴 | ✅ 合同已钉死（SKILL 词步骤改为「参照曲画像先行、依据必须引画像/原话」）；本项目已用耳朵证据改词重检 |
| ⑤ | G1 | 缺"对参照曲本体出绝对属性笔记"的一等入口：`music_listen_omni --absolute` 只吃候选清单，参照曲的耳朵事实只能靠 A/B 笔记里十次顺带描述拼出来。本次问题④若先有该模式，一轮参照曲单听即可拿到体裁事实，可省 62 首错向检索 | 给 listen_omni 加 `--reference-note`（或对参照曲直接跑 absolute）产物入经验库档案，检索词卡规则改为"有参照曲时先出参照曲绝对笔记再出词" | 🟡 | ✅ 已修（`--reference-note` 出《参照曲风格画像》：长参照曲 2–3 段节选合听；SKILL 2.6/2.7 已钉为出词前置；两测） |
| ⑥ | G1 | `music_listen_omni` 的笔记复用缓存只认 `--absolute` 模式；A/B 模式对同（参照曲×候选×提示版本）组合必重听。v0.2 排序短名单与 v0.1 高度重叠，10 对旧组合白白重烧模型调用 | A/B 笔记按 (sha, referenceSha, promptVer) 入 ledger 可复用（A/B 相对评价仍不做库事实，只做会话内缓存） | 🟢 | ✅ 已修（项目内 A/B 缓存 sidecar+测试；不进经验库红线不动） |
| ⑦ | G1 | `music_search_freesound.py` 的 `--max-results`（默认 8/词）没有真正封顶：脚本按 `page_size=max_results*3` 向 API 要 24 条/词，全部接收后只做去重+时长过滤，从不截断回 8/词——4 词理论上限 32 首实际抓了 62 首并全部下载预览。用户记忆中的合同（"每词前几首、短名单挑"）被静默放宽近一倍 | 检索通过滤后每词截断到 max_results 再下载；回显"每词 N/上限 M"让封顶可见 | 🟡 | ✅ 已修（每词真截断+候选清单 perQuery 逐词封顶审计+测试） |
| ⑧ | G1 | 编排漏步：合同要求 G1 找乐"先查经验库、已建档曲目零成本带出笔记当候选"，我虽 query 了库却把库内贴合方向的旧曲（工业电子3首/史诗管弦6首，均 uncleared-platform-catalog 但本片内部预览可用；LOW-PHONK 已清权）当"对本次无用"直接跳过，未并入候选池、未进回显卡 | 查库命中的曲必须作为第一批候选进池（红灯照显），外部检索是库不足时才补；回显卡须区分"库内零成本候选/外部新检候选" | 🔴 | ✅ 合同已钉死（SKILL 查库步骤改为「查询结果必须进池进卡」）；本项目已补端库内候选 |
| ⑨ | 工程 | 修⑦时发现测试假绿：`test_music_sourcing.LicenseClassificationTest` 单文件独立跑会因 sys.path 未含 scripts 而报错——400 基线全绿只因合跑时别的模块先注入了路径（顺序依赖）。已注入路径修复并补跑单文件验证 | 回归口径明确"每测试文件单跑"；跨用例的 sys.path 副作用视为缺陷 | 🟡 | ✅ 已修（注入 + 单文件复验） |
| ⑩ | G2 | 问题②在转写链路复发：`local_transcribe.py` 用宿主默认 python3 跑同样报 `faster_whisper is not installed`，实为 3.12 venv ABI 不匹配；换 WorkTool venv python 后即通 | 与⑤同修：两个本地模型脚本（analyze/transcribe）的 blocked 文案统一区分"依赖未装/env 未设/解释器不匹配"，并打印当前解释器与建议命令 | 🟡 | ✅ 已修（2026-09-23 批次2-3：local_transcribe blocked 文案区分 env 未设/ABI 不匹配，打印当前解释器与 venv 建议，与②同模式） |
| ⑪ | G2 | 事实核证通道无合同指引：宿主 WebFetch 连续超时、Wikipedia/Fandom 本机网络不可达、百度百科触发安全验证、必应中文 SERP 被品牌词干扰且部分结果被过滤——四处碰壁后才靠 `bl search web`+BWIKI 直采跑通（核证 3 条主张烧了 5 次试错）。G2 证据合同只规定"事实必须回指来源"，没给可用通道与降级链 | 证据准备合同补"外部事实核证通道预览→降级链"（境内可达百科/bl 联网搜索优先），失败通道留 raw 快照作审计；编排不许在死链路上重试超 2 次 | 🟡 | ✅ 合同已钉（2026-09-23：g2-script-review-gates 新增「外部事实核证通道」节——bl search web→领域百科→WebFetch 降级链、死链≤2 次、raw 快照+失败记录入核证快照/） |
| ⑫ | G1→G2 | 选定 BGM 时长与目标 timeline 不核对，冲突拖到 G2 蓝图才爆：LOW-PHONK 整轨 177.875s < G0 登记目标 180s，`music_align --blueprint` 直接 blocked（禁循环/填充，合同正确）。分析报告里 durationMs 明明有值，bgm-choice/G1 确认卡却没人拿它对目标时长说一句"够不够铺"；用户选曲时完全不知道差 2.1 秒 | bgm-choice 关单时机器比对：trackDurationMs < 目标时长下限时在确认卡/回显亮 warning（提示要么缩 timeline 要么换更长曲），不许静默放行到 G2 | 🟡 | ✅ 合同已钉（2026-09-23：p0-c-pipeline SKILL bgm-choice 条目——关单必用登记件 trackDurationMs 机器比对目标下限，短则 warning+两出路，静默放行=编排漏步；本项目已按 177s 缩 timeline 继续） |
| ⑬ | G2 | 排版稿首层估速无校准数据：longtian_v3（cosyvoice-v3-flash）实测约 4.4 字/秒（含标点计数），首稿按 4.25–4.45 估、总长差 ~5%，28 句首排溢出 timeline 1s+、累计意图偏差最大 5.9s，触发 3.5 改稿循环（缩 1 句删 1 句）才归位。循环本身按设计工作（实测=唯一事实、回改稿不硬凑），但每次都会白烧一轮 | 配音清单沉淀每 voice×model 的实测 cps 参考值（002 已有数据可回算），G2 首层排版直接取用；voice 首次使用前先合成 1–2 句拿语速再铺全稿 | 🟢 | ✅ 合同已钉（2026-09-23：g2-script-review-gates「语速校准」节——首版排版先查实测 cps，未用过的 voice 先合成 1–2 句） |
| ⑭ | G2→G3 | 配音清单字段名无合同钉死：002 先例用 `startMs/durationMs`（music_align --voice-brief 只认这个），本跑 G2 自创 `measuredStartMs/measuredDurMs`，握手到 G3.5 才被 music_align 拒收（"every sentence needs integer startMs/durationMs"）。报错清晰、修复便宜，但 schema 靠考古先例文件才摸清 | G2 配音清单 schema 写进 g2-script-review-gates/evidence-contract（字段名+必填项），或 validate_g2_decision 检查下游合同字段存在性 | 🟡 | ✅ 合同已钉（2026-09-23：g2-script-review-gates 配音清单 schema 字段名钉死 startMs/durationMs（与 music_align --voice-brief 一致），禁自创同义字段名；本跑已补别名修复） |
| ⑮ | 政策 | 用户裁决（2026-09-22 G3 S 卡后原话）：「以后选选项能自动推荐一个，这样用户之后直接确认即可，就像前面几个节点那样，然后默认是缩短时长而不是补素材」——覆盖旧「身份/验收永不预选」条款（09-11 单卡合同）；充分性三选一的默认从此=缩短/改写回 G2，补素材降为用户点名选项 | choice-cards 系列文档统一改写：所有用户可见选项卡必须带【默认=推荐｜理由】；归属记录纪律不变（验收默认 vs 自选仍要如实区分）。待测后合同回填批次处理 | 🟡 | ✅ 已修（2026-09-23 批次2-3：final-echo-style-guide v1.2——全卡必带【默认=推荐｜理由】、充分性缺口默认=缩短/改写回 G2；归因区分纪律不变） |
| ⑯ | G3 | 网格连续性合同只在转场校验器里存在：v0.3 按"段输出=口播时长、句间留白不归属任何段"设计，transition_validate_plan 一把拒掉全部 27 段（要求 0..total 连续网格）。合同模型本身正确（音不动画面服从），但 g3-edit-plan 的 plan-contract 从没写"段输出区间必须连续覆盖成片"，validate_g3_plan 也不查——只在转场校验时摊牌，等于让编排先写错一版 | plan-contract 明文"连续网格"为硬约束 + validate_g3_plan 同步检查连续性；留白归属规则（边界=留白中点）写进合同示例 | 🟡 | ✅ 已修（2026-09-23 批次2-3：plan-contract「连续网格」硬约束段+中点归属示例；validate_g3_plan 机器强制 approved_for_g4 连续覆盖 0→总时长，附拒绝测试；本跑已改 v0.4 连续网格过双校验） |
| ⑰ | G3 | 黑场入/出在口播驱动网格上物理不可行却无前置说明：N01 口播起于 0:00.070、N27 止于成片终点，任何 ≥100ms 黑场窗口必压口播（红线"音不动画面服从"），v0.3 卡上"黑场入500/黑场出800"到转场校验才爆。词表文档说黑场="片头片尾时间内的渐入渐出、零手柄需求"，未写"仍须成片首/尾存在≥D 静默停顿" | 合同给黑场加前置条件（首/尾停顿≥D）+ 标准出路（追加片尾静默并重跑 music_align）作为可选路径写明，别让用户在卡上才看到不可行 | 🟡 | ✅ 合同已钉（2026-09-23：transition-contract 黑场入/出两行——前置条件"成片首/尾存在≥D 静默停顿"+标准出路"追加片尾静默并重跑 music_align"；本跑终审卡已如实摊牌，默认=硬切） |
| ⑱ | G3 | 低帧率探针瓦片多次误导窗口定界：1/2/4fps contact-sheet 上把 403s 读成红机+爆炸（实为独角兽）、417s 读成红机（实为白机）、492.5s 读成红机（实为杰斯塔）、439s 读成舱体（实为爆炸）——共 4+ 次，全部靠实际抽帧纠正 | G3.4 视觉验证步骤明文：瓦片只用于定向，窗口起止/切点判定必须以实际抽帧为准（4fps 也只作扩展区粗证） | 🟡 | ✅ SKILL 已钉（2026-09-23 批次2-3：g3-edit-plan G3.4——瓦片只用于定向，窗口起止/切点判定必须以实际抽帧为准；本跑 v0.4 全部窗口以实际帧+4fps 扩展探针定界） |
| ⑲ | G3.5 | 字幕断行两处小坑：① 校验器宽度模型（F/W=1.0、半角=0.55、可用宽=W−ML−MR）与编排直觉"26 字/行"不一致，首稿 33 单位长行被"unbreakable run"拒（autoWrap=false 下无空格即不可断）；② ASS 只到百分秒，SRT 保留毫秒会触发"re-timed"拒收（校验器报错里引的 ㊍ 是 002 清单第 14 条，非本清单编号；本行原稿误抄作 ㉘，2026-09-22 已正），须两轨同 floor 到 10ms | subtitle-expert 暴露可复用的 wrap 助手（或合同写明宽度模型算法）+ 合同注明"SRT 从 ASS 时间派生（同精度 floor）" | 🟢 | ✅ 合同已钉（2026-09-23 批次2-3：layout-contract 规则1 写明宽度模型算法 F/W=1.0em/半角0.55em、可用宽=W−ML−MR，以校验器 char_units/wrap_line 为唯一权威；规则2 已有 SRT 同源派生+同精度取整；本跑按校验器同模型手写 wrap+floor 后全绿） |
| ⑳ | G3 | 节点耗时结构问题（用户 2026-09-22 实测追问"为什么工作这么久"）：昂贵感知检查（27×3 帧视觉验证）跑在便宜结构检查（转场网格连续性）之前，v0.3 全量验证完成后才被 transition_validate_plan 拒网格→81 帧整体重抽重看一轮；且 v0.4 只有部分窗口移动，未动窗口也白白重抽。叠加⑱瓦片误读返工与校验器合同撞错考古，G3 约 40% 耗时属可避免 | ① g3 SKILL 执行顺序改为"结构校验先行"：连续网格+转场可行性（纯数学，零抽帧）→ 再进视觉验证；② 抽帧按窗口指纹缓存，计划改版只重抽位移窗口；③ ⑯ 合同明文后此条自然消一半 | 🟡 | ✅ SKILL 已钉（2026-09-23 批次2-3：g3-edit-plan G3.5——结构校验先行（连续网格+转场可行性纯数学零抽帧）→再进视觉验证；抽帧按窗口指纹缓存、改版只重抽位移窗口；⑯ 已同步机器强制） |
| ㉑ | G3 | 编排把"硬切=字段缺省值"过度解读成"用户不说不做、编排不主动设计转场"（口头禅来自 09-21 会话记忆压缩，合同原文无此语；contract:15 只说缺省值，SKILL:34 只禁**专员脚本**代选，contract:41 流程=编排提案→逐切点人工批准）。后果：转场提案层空转，用户点名才补，恰好架空本跑要测的能力。用户 2026-09-22 澄清：「默认硬切，然后可以用别的转场动效换，我没说硬切写死」 | g3-edit-plan SKILL 转场步骤明文："编排必须基于逐切点素材观察主动出转场设计提案（含每种动效的编辑理由），专员脚本只做可行性裁决不做设计"；会话记忆口头禅不得当合同引用 | 🟡 | ✅ SKILL 已钉（2026-09-23 批次2-3：g3-edit-plan G3.5——编排必须基于逐切点素材观察主动出转场设计提案（含编辑理由），专员脚本只做可行性裁决不做设计；本跑已现场纠正为编排主动提案） |
| ㉒ | G4 | 装配器 xfade 时基缺陷（程序性 bug，当场修）：叠化位于长 concat 链中段时，xfade 第一输入=concat 输出（tb=1/1000000）、第二输入=单段 fps 输出（tb=1/30），ffmpeg 7.0 直接拒绝（"First input link main timebase do not match"），成片零输出。批②冒烟只测了 2~3 段迷你片（xfade 两侧都是 fps 直出 1/30）→ 测试盲区。本次实片（27 段链、叠化在第 16 边界）当场引爆 | xfade  junction 两侧统一 settb；批测补一条"叠化在长链中段"用例 | 🔴 | ✅ 当场修（见下）；✅ 批测补例完成（2026-09-23 批次1：6 段链中段 xfade e2e 复现用例 + build_video_chain settb 单元断言 + 无转场路径零污染断言） |
| ㉓ | G3/G4 | 描述性字段两处事实错（成片帧目视当场逮住，网格零影响）：① 全 27 段 sourceCaptionTreatment 把左上水印 UP 主名写成"shini都文玩鸭子"，成片多帧清晰显示实为 **"shin超次元响子 bilibili"**；② 封面合同 note 写"432500ms=推进翼全开"，实为大型步枪+金袖章镜头（翼段 434.0s 起，帧本身是好封面但描述错）。G3 视觉验证只核"画面内容支撑口播"，未复核自己写的包装描述——描述字段无机器对账 | 包装描述字段（水印名/封面帧描述）纳入 G5 分发边界披露前逐字核对；G3 卡回显时描述性文字与帧证据同屏，便于人眼抓错 | 🟡 | ✅ 本跑已修入 v0.6（水印名/封面 note 勘误，随用户 确认G3 重批，未静默改）；✅ 合同改进已钉（2026-09-23 批次2-3：g3-choice-cards 新增"包装描述与帧证据同屏逐字列出"、g5-choice-cards 人类确认覆盖项新增"包装描述字段逐字核对"——描述字段无机器对账，事前同屏+分发前人工关口双堵） |
| ㉔ | G4 | 词表映射观感问题（待用户看完成片裁决）：合同把"叠化"映射到 xfade=dissolve，而 xfade 的 dissolve 是**噪声抖动式**混合（中点帧呈颗粒状），平滑交叉淡化其实是 xfade=fade。批②冒烟只验"非黑帧真混合"未验观感。本片中点 110.271s 目视=颗粒感混影 | 若用户要平滑叠影：合同映射改 dissolve→fade（专员一行改+批测）；若接受颗粒风格=不动。G5 必检帧已含中点帧 | 🟡 | ✅ 已裁决落地（2026-09-22 用户 确认G4=接受默认）：叠化映射改 xfade `fade`，指令 v0.2（CBBEC226…）→ 候选 v0.3，三处中点目视=平滑双影颗粒消失；合同/指令脚本/能力探测/校验门+四套测试同步，38 测试文件单跑全绿 |
| ㉕ | G4 | 口令白名单不认自然语序：用户回「OK，那这个G4我确认了」（意图零歧义：节点名+确认动词同句），normalize_confirmation 因正则锚定开头/只认裸"确认"而拒绝，门禁卡住等重打。报错文案只说"must be 确认 G4"，未告诉用户"你的话我理解了，但机器合同要逐字口令"。G1 先例（确定G2 同义词）说明白名单本就在追赶真实用语 | 白名单扩"确认/确定 与节点号同句且无他节点引用"的短句形（verdict 归一+verbatim 原话留痕机制不变）；拒绝文案改为"语义已识别，合同需精确口令：请回 确认G4" | 🟡 | ✅ 已修（2026-09-23 修复批次1：normalize_confirmation 扩"单一确认从句"自然语句形——**不是模糊匹配**：按读点拆句后要求"含确认/确定的从句全句唯一＋该从句只点名当前节点＋任何位置无条件/含糊/祈使词（如果/先别/但/帮我/吗/还是…）"三关齐过才放行，宁可漏放不误拒；拒绝文案改为含精确口令指路。批测补 2 例（自然句接受+含糊句拒绝），17/17 绿。09-22 晚 G5 关单时旧代码在跑，按句首精确口令人工提取关单、全句原话入批准记录——本修复覆盖该场景） |
| ㉖ | G3 | 终审卡"确认G3"语义与默认推荐新规自相矛盾（用户 09-22 实测指出）：编排提案卡把 3 处叠化标为【默认=推荐①】，收尾句却写「直接 确认G3 按现状 v0.4（1 处叠化）关单」——同一张卡两个"确认"指向不同批准对象。用户按 09-22 新规直觉（确认=接受默认推荐）回 确认G3，实际被记录为"未批①按现状关单"，成片少两处叠化，G4 关单后才发现分歧，触发 reopen 重做。归属：卡片起草错误（编排），非用户误读 | choice-cards 合同明文：节点终审卡存在待批提案时，唯一关口令=接受默认推荐；"按现状/放弃推荐"必须另给显式不同口令（如 确认G3-现状），禁止同卡双义 | 🔴 | ✅ 合同已钉（2026-09-23 批次2-3：final-echo-style-guide v1.2——终审卡存在待批提案时唯一关单口令=接受默认推荐；"按现状/放弃推荐"必须另给显式不同口令，禁止同卡双义；本跑现场纠正：reopen→v0.6 三叠化→重走 G3/G4 门禁） |
| ㉗ | 状态机 | reopen 路径覆盖不全：`reopen` 只允许 currentNode==G3（回 G2）、`reopen-g3` 只允许 currentNode==G4（回 G3）——**G4 关单进入 G5 后，发现 G3 批准范围分歧（本跑 ㉖ 现场）无任何合法回退命令**，只能手改状态文件（被禁止）或整案重开。修正案只画了"G3 活跃期"的往返，没画跨节点回退 | reopen-g3 接受 currentNode∈{G4,G5}，回退时连带重置 G5（清门禁/批准，产物留盘作审计）；批测补"G5 期回退 G3"用例 | 🟡 | ✅ 当场修（本跑 ㉖ 纠正需要）；✅ 批测补例完成（2026-09-23 批次1：G5 期 reopen-g3 连带重置用例） |
| ㉘ | G4 | reopen 重跑产物版本覆盖：v0.6 重批后 transition_directive 重新生成执行指令时**写回原文件名** `G4-转场执行指令-v0.1.json`（哈希 DB88199A…→CA69D5C8…），旧指令文件从磁盘消失，v0.1 装配记录里的旧哈希成孤儿——违背本仓"产物留盘作审计"惯例（计划/候选/验证都新建版本号，唯独专员指令产物不带版本递增） | reopen 重跑时专员指令/审计产物同样按版本递增落盘（文件名含计划版本号或自动递增），旧产物保留 | 🟢 | ✅ 已修（2026-09-23 修复批次1：directive/能力档产物 next_versioned_path 自动递增、永不覆盖，SKILL/合同同步；批测 CLI+单元各补 1 例） |
| ㉙ | G5 | 门禁收据合同只活在代码里：cardType 精确值（g5_delivery_review）、checklist 五 id 精确集合（delivery_manifest/qa_validation/playback_review/distribution_boundary/check_frames）、evidenceRef 单数字符串——全部只在 review_gate.py 常量与校验逻辑中；media-qa-delivery SKILL 只说"登记为机器可验证的 G5 门禁收据"，卡片合同文档未列字段。编排照 N7 先例起草仍三撞三轮回（cardType 名错→id 集错→evidenceRefs 复数错） | choice-cards/review-gates 文档给每节点收据列"cardType+checklist id+evidence 形态"三行速查表；与 ⑭（配音清单 schema 考古）同族：下游合同字段必须写进上游产出合同 | 🟡 | ✅ 已修（2026-09-23 批次2-3：g5-choice-cards 新增收据"cardType+checklist id+evidence 形态"三行速查；pipeline-state-contract 同步 ㉕ 自然句口径；⑭ 同族已各自钉死） |
