# 独角兽高达短版 - 内部验收草案

本包用于本地内部验收，包含横版固定字幕条成片、字幕参考、封面、三段章节预览和完整来源追溯。

授权状态为 `internal_test`，分发限制为 `not_for_distribution`。不得上传、公开发布或作为已获发行授权的素材使用。

先播放 `final-video.mp4`，再根据 `metadata-validation-report.json` 的警告完成验收。`subtitles.srt` 是审核口播的可编辑字幕参考；成片中的字幕已按固定底部字幕条样式烧录。
