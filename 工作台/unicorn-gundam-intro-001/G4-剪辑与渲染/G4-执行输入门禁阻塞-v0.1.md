# G4 执行输入门禁阻塞 v0.1

## 结论

未开始裁切或渲染。G4 准备器拒绝接受当前已批准 G3 计划作为可执行输入，原因是计划合同不一致；不得以手工猜测的验证字段绕过。

## 已核对事实

- `pipeline-state.json`：G3 已完成且当前节点为 G4。
- `G3-完整剪辑计划-v0.2.json`：记录 13 段时间码与摘要级 `visualStatus`，但状态为 `review_required`，并不含 G4 所需的 `approved_for_g4`、每段 `visualVerification.status`、帧清单引用和画面观察字段。
- `G3-画面验证帧/G3-visual-verification-frames.json`：存在 13 段、每段起/中/终三帧，但其 `planRef` 指向旧 `G3-完整剪辑计划-v0.1.json`。
- `G3-候选验证记录-v0.1.json`：也是旧审查记录，含 `review_required` 和与当前 v0.2 不完全一致的候选说明。

## 所需修复

生成一份与已批准 G3 v0.2 一一对应的、不可改变时间码或主体限制的执行计划，至少包含：

- `status: approved_for_g4`，并回链现有 G3 审批记录；
- 13 段的源/输出时间码与 G3 v0.2 完全一致；
- 每段明确的三帧路径、观察、风险标记和 `visualVerification.status: verified`；
- 该执行计划与旧 v0.1 验证记录之间的差异说明，特别是 seg-009 的驾驶员用途。

在此之前，G4 不得生成分段、预览或成片。
