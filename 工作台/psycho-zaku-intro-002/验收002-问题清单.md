# 验收录制 002 问题清单（2026-09-17～18，G0–G5 全链实录）

设计冻结期内只收集不修改；每条记：现象、复现位置、临时绕法/已修状态。载体 `工作台/psycho-zaku-intro-002/`，六幕门禁收据同目录。

## ① 新对话开场凭记忆转述旧进度＝"被监视感"（交互纪律）

- **现象**：验收首载体 001 开新对话，agent 开场直接转述旧 G0 进度，用户"这个全部删掉吧，从头开始跑"——001 整目录删除重跑。
- **处置**：纪律已入记忆（fresh-session-zero-start）：开场只报磁盘事实，由用户定续接/从零。**未落进 SKILL/AGENTS 成文条款**，待裁决。

## ② G0 卡 BGM 通道预告（已修，本跑验证生效）

- 09-17 实测反馈→`19c862c` 补通道预告；002 G0 卡实跑含通道行，用户随后点名 Freesound。闭环。

## ③ Freesound 检索 0/42 过线：时长口径对短素材过严（G1）

- **现象**：目标成片 3–10 分钟口径下检索全部候选时长不达标（42 条全拒），最终走网易云+委托选定。
- **意义**：正是 09-17 挂账的"对外轨首批活测候选不足风险"的真实数据，回收留对外阶段裁决（放宽时长画像 or 候选拼接策略）。

## ④ 选曲循环 18 首全否后"委托选定"入卡（G1）

- **现象**：模型试听全否循环多轮后用户原话"你随便挑选一首"才脱出。委托选定路径可用但依赖用户主动喊停；候选池耗尽时卡片应主动给"委托/放宽画像/无BGM"三选一（recommend 引擎已有类似机制，人工选曲循环未接）。

## ⑤ validate_g3_plan 对结构错误报 KeyError traceback，非批量 errors[]（G3）

- **现象**：G2 证据清单把事实条目混进 sourceEvidence（缺 assetId）→ 校验器抛 `KeyError: 'assetId'` 裸 traceback。
- **违背**：N8 ㉕"一次报全"的 SoftFail 纪律只覆盖了字段校验路径，条目结构归一在数据加载层就炸。
- **临时绕法**：按 zaku/tiger 先例重构证据清单 v0.2（sourceEvidence 仅媒体条目，事实条目移 webSourceEvidence，additive+revisionNote）。

## ⑥ plan schemaVersion 必须 "0.1"，与文件名版本直觉冲突（G3，轻）

- 文档版本走文件名（v0.2.json），合同 schemaVersion 恒 "0.1"——本轮误升 0.2 被拒后才发现。合同文档应显式写"版本≠schemaVersion"。

## ⑦ record-review 快照过期无提示（状态机）

- **现象**：修改门禁收据（补 basisRefs）后直接 approve → 报"basisRefs 缺 X"，实为 state 内旧快照。须重新 record-review 刷新。
- **建议修法**：approve 比对收据文件与快照 renderedAt/basisRefs，不一致时报"快照过期，请重新 record-review"。

## ⑧ CJK 断行合同缺口：布局校验器与渲染器能力不一致（G3→G4，**用户可见级**）

- **现象**：validate_g3_subtitle_layout 的保守宽度模型假设"任意字符可断"，但本机 libass 构建**不自动断 CJK 长行**→ G4 首轮成片检查帧 N02/N04/N07 字幕左右被裁。
- **绕法**：ASS 显式 `\N` 语义断行（一条口播一排版块、两行均衡、≤27.5 全角当量），SRT 同步转真实换行；校验器改前先按现状通过=校验器绿灯不保证渲染正确。
- **待裁决修法**：布局校验器加"renderer 能力档"（autoWrap=false 时强制显式断行存在），否则此坑在对外素材上会复发。

## ⑨ g4_assemble 默认 fps=24 不读计划（G4）

- **现象**：计划 editPlan.fps=30，装配器不吃计划、默认 24，装配记录里规格静默错位，复核时抓到，显式 `--fps 30` 重装配（+13ms 帧取整合同内）。
- **建议修法**：`--from-plan` 默认继承 editPlan 的 fps/分辨率，显式参数才可覆盖。

## ⑩ 字体预检红线生效实录＋候选表缺文档（G4，正面证据）

- Hiragino Sans GB.ttc cmap format 14 → 预检 blocked 拒绝开工（N8 ㉛防线的验收镜头）；Songti.ttc 是靠逐个试出来的，字体推荐清单（含"禁带空格路径"）应进 runtime-dependencies.md 正文。

## ⑪ 像素越界检测假警报（G4 检查帧）

- 亮色肩甲内容触发 RIGHT-CLIP 阈值，目视字幕完整。检测器应限定不透明描边/底带 ROI 或提高内容区分度，否则每次人工复核成本。

## ⑫ g5_validate_delivery 字段要求与模板/文档有差（G5）

- export-config 需 projectId+authorization、qaReport 需 finishedAt、traceability 需 output/sourceStartMs 精确字段名——本轮靠读校验器源码补齐。**核对点**：㉝ 新增的 delivery-bundle-schema.md 是否覆盖全部 REQUIRED/CONTRACT_FIELDS；未覆盖即补，SKILL 引用 schema 而非凭记忆。

## ⑬ manifest pending 阶段即要求 finishedAt：语义含糊（G5，轻）

- CONTRACT_FIELDS 不分 status 一律要求非空 finishedAt。本轮登记为"机器 QA 收口时刻"并在卡上如实说明；合同应定义 finishedAt=质检收口 or 交付关单，二选一。

## ⑭ mpdecimate 对动漫平涂过敏：警告口径靠临场（G5）

- 2842→1741 帧被标记，合同"如实记警告"条款方向正确（本轮未报通过），但 QA 模板缺动漫素材的判读指引（同源区间互斥机验+抽帧目视=标准解释链），建议进 media-qa-delivery references。

---

**验收面亮点（供成片镜头）**：G4 三处真实缺陷的发现—修复—复验闭环（⑧⑨⑩）全部留档 failure-samples；无听觉能力在 W5 如实声明并交由用户亲耳复核；`确认G` 缺节点号被状态机正确拒绝（G1）；警告五条逐条呈卡、确认交付≠逐条接受语义走完。

---

## 测后裁决执行台账（2026-09-21，用户口令"直接做吧"，三批收口）

全量回归 8 套全绿（video-edit-plan 99、music-expert 96、p0-c-pipeline 16、media-qa-delivery 15、local-video-render 37、其余照旧）。

| # | 处置 | 落点 |
| --- | --- | --- |
| ① | 开场纪律成文：operator-dialogue 第 6 条 + AGENTS 路由第 6 条（只报磁盘事实、续接/从零用户定） | `abaf3c1` |
| ② | 已修（`19c862c`），本批销账 | — |
| ③ | 时长口径两档：硬档零候选→一次放宽重试+`durationShortfall` 显式标注，循环/拼接/放宽交卡片决策，绝不静默凑时长；源码守卫测试入册 | `e2c21b4` |
| ④ | 候选耗尽四选一（委托/放宽/换通道/无BGM）卡片主动脱出，委托按"用户委托"如实归属 | `e2c21b4` |
| ⑤ | 证据清单结构错误改批量 errors[]（sourceEvidence 归一在加载层，含"事实条目应去 webSourceEvidence"指路），回归测试锁死 | `7ba993c` |
| ⑥ | "文件版本≠schemaVersion"唯一口径入 g3-plan | `abaf3c1` |
| ⑦ | approve 前重读收据比对快照：过期→"changed since record-review"、失效→"no longer valid"，均拒并指路重录 record-review | `7ba993c` |
| ⑧ | 布局校验器加渲染器能力档 `autoWrap`：**缺省=保守档**（不可断 CJK=本机实况），超宽不可断段直接判违规；绿灯=按所选能力档可渲染。合同文档同步 | `7ba993c` |
| ⑨ | `editPlan.fps` 经 manifest `targetFps` 贯通 prepare→render→assemble，显式 `--fps` 才可覆盖，装配记录含 `fpsSource`；非法 fps 在 prepare 即拒 | `7ba993c` |
| ⑩ | drawtext 字体推荐清单（Songti/PingFang+禁空格路径+反面清单）入 runtime-dependencies | `abaf3c1` |
| ⑪ | 字幕越界判读纪律：只对描边/底带 ROI 出结论，亮色内容非证据，告警须目视确认——入 qa-contract | `7ba993c` |
| ⑫ | schema 全量对账：REQUIRED 12 文件、CONTRACT_FIELDS 12 字段、边界栏均已被 schema 覆盖；补 export-config `authorization/distribution` 必填条款 | `7ba993c` |
| ⑬ | 裁决 `finishedAt`=机器质检收口时刻（非交付关单，关单以 state `确认G5` 为准）；pending 状态允许暂缺，校验器兑现合同 | `7ba993c` |
| ⑭ | 动漫平涂 mpdecimate 判读链（源区间互斥机验+抽帧目视=标准解释链）入 qa-contract | `7ba993c` |

**批外追加（同用户口令）**：music-expert 环境变量通用名补齐+检索器 SSRF URL 白名单守卫（Mimosa L3 拦截后修复）＝ `b7cc9c2`。字幕系四条（⑧⑪⑭+㉖㊍旧账）的归属整合候选 subtitle-expert：章程草案见 `docs/subtitle-expert-章程草案-v0.1.md`，剥出与否待用户拍板。

**同日追加**：用户拍板"万事大吉了我们就弄 subtitle-expert 了"→ **subtitle-expert 已剥出立册**（`skill/subtitle-expert/`，pipelineNode: support，花名册登记）：布局校验器自 G3 迁入、style-contract 自 G4 迁入、新增渲染器能力档探测与 layout/qa 两合同；章程三块"新引擎"防过度设计未建，出入见章程文末《执行记录》。⑧ 的归属随迁：能力档语义与接线说明书唯一事实源移至 `skill/subtitle-expert/references/layout-contract.md`。

**同日再追加（用户复审"剥离就要彻底"）**：归属复查抓到三处不彻底全部改造——G4 内嵌的章节卡 cue 修剪迁入专员 `subtitle_trim_cues.py`、G5 私有 SRT 正则改产物握手 `subtitle-srt-check.json`、生成纪律入机验（`--srt` 逐 cue 对时对文＋`--source` 口播块对齐＋重叠拒拆层）；另修探测脚本 libass 版本误解析真缺陷（configuration 行 flag 被当版本号）。⑧㊍ 的防线以 002 实产物活体复验：逐 cue 对时拒 10× 漂移、口播块 13/13 对齐、trim 后批准源零改动。
