# 日报 2026-09-11 —— N8 毕业考：tiger-intro-001 G0–G5 全链闭环

## 一、任务定位

N8 毕业考首个真实项目全流程实测：《高达08小队》MS-07B3 老虎改机体介绍片，B站公开（首个 public_bilibili 项目）。源素材为 14 分钟快切二创 AMV（1436×1080），风格参照《冷战奇迹》档案纪录片式，BGM 网易云试听件《Infinity（鲫虞豆腐汤remix）》用户声明自担。设计冻结：只收集问题不中途改管线（bug/文档缺口允许即时修）。

## 二、G0–G2（上午–午后）

- G0 素材包入册；路由纠偏：正式项目迁回仓库 `工作台/`（问题⑯）。
- G1 方向卡（标题A/叙事角度1/史诗基调）+ BGM 全链闭环（找乐→试听→登记→用户自担声明），收据机契约补进合同（问题⑳即修）。
- G2 证据链 + 18 句口播全配音实测 120.1s（longtian_v3 音色）；单卡样式 v1.1 首节点实跑；收据路径解析基准成文（㉑即修）。commit 04ec350 / 7f3bf86。

## 三、G3 剪辑计划（下午–傍晚，本考最重节点）

- 快切 AMV 视觉核验方法论收敛：固定网格系统性错切 → scdet@0.10 镜头检测（1447 切点→581 子镜头）→ 子镜头中帧 → 逐窗声明核验 → 209 帧观察账本 + 50 段逐段三帧抽验。HLV 发射镜头靠定向探针找到（68.1–74.4s 白光柱）。
- 50 段全局无重叠时间表（每毫秒素材唯一）、口播切片连续无隙、BGM 对齐 35/35 吸附、字幕 19 cue 固定版式合同、24 段源字幕 mask 批准表。
- 三校验器全绿；关单词汇修正（mappingMode/timelineReview 的 review/approval 模式不对称，问题㉗）。commit efcda53。
- 用户质询 G3 耗时：如实归因——主因是快切素材逐帧核验的固有难度，harness 贡献是单错报告校验器与合同缺口（㉓–㉗）。

## 四、G4 本地渲染（晚间）

- 固定链 g4_prepare→g4_render→g4_validate→g4_assemble 全跑通：50 段切片 1920×1080/24fps、mask 合同执行、配音轨归一（-24.4→-14.3 LUFS 实测）、BGM 按混音合同（bed -12/duck -6/offset 53636）、ASS 压制。
- 7 帧抽验全过（水印保留、无双字幕、N16 拆分 cue、结尾 CTA）。
- 守卫立功：G4 目录名违反布局合同被 approve 拦截（问题㉚：入口脚本不校验 G4_ROOT，守卫太晚）。commit 4aa12b6 / a0efe61。

## 五、G5 质检与交付（深夜收官）

- 交付包 v0.1：14 类组件 + 50 段追溯 + 4 章节切片；`g5_validate_delivery --media` valid 0 错误；BGM 哈希链审计 12/12（中途抓到注册链哈希漂移并修复）。
- 封面按 coverWarning 重制：裁除水印构图 + libass 标题（首版 drawtext 简体缺字，问题㉛）。
- 黑帧 3 段命中如实列警告（源片暗场景，人工确认非故障）；重复画面检测器缺失按合同记警告不报通过；failure-samples 5 条真实失败。
- 用户「确认G5」（选项1 确认交付，警告保持可见）→ human-review approved → manifest 复验 valid → **currentNode=completed，项目闭环**。
- 分发边界锁定：非商用二创生态；商用需换 BGM 重走 G4。

## 六、问题清单收官统计（34 条）

真 bug 9（含高危⑤分发边界静默洞）｜合同/文档缺口 9｜校验器/CLI 行为 5｜样式流程（已落地v1.1）3｜模型与曲库供给事实 4｜方法论成文 1｜护栏设计（㉙口令独立性不可机验）1｜路由纠偏 1｜操作事故 1（㉞：mv 覆盖致清单 ①– 一度丢失，git+会话rollout 全文找回，无最终丢失）。测中已修/落地 8 条，待测后统一裁决 26 条。

## 七、遗留与下一步

- 26 条待裁决批次（重点：⑤红线洞、㉕㉗㉜㉝ 校验器批量报错+schema 成文、㉙ 口令护栏重设计）。
- 17 笔本地 commit 等用户口令 push。
- tiger-intro-001 与 zaku-intro-001 并列为管线基准案例：zaku=internal_test 基准，tiger=public_bilibili + 快切 AMV 基准。
