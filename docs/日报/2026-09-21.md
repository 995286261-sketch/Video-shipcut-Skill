# 日报 2026-09-21（002 裁决 14 条批次收口 + subtitle-expert 剥出立册）

## 1. 今日定性

两线收口日：①验收 002 问题清单 **14 条测后裁决全部处置**（三批+收口共五笔，用户口令"直接做吧"），已随用户口令 push；②"底座+插件"定案落地第二个插件——**subtitle-expert 剥出立册**（用户口令"push，然后万事大吉了我们就弄 subtitle-expert 了"）。

## 2. 002 裁决批次（`b7cc9c2`→`7ba993c`→`e2c21b4`→`abaf3c1`→`dc9b2f1`，已 push）

- **批 A 机器 bug**：⑧ 渲染器能力档 `lanes.narration.autoWrap`（缺省=保守档，不可断 CJK 超宽即判违规——绿灯必须建立在所选能力档之上）；⑤ validate_g3_plan 批量报错；⑦ approve 重读收据比对快照（changed since record-review）；⑨ editPlan.fps 经 manifest targetFps 贯通 prepare→render→assemble（含 `fpsSource`）；⑪⑫⑬⑭ 合同兑现（finishedAt 语义、export-config 边界字段、mpdecimate 判读链、ROI 判读纪律）。
- **批 B 找乐策略**：③ 时长两档（硬档零候选→一次放宽+durationShortfall 显式标注，决策权在卡）；④ 候选耗尽四选一主动脱出。
- **批 C 纪律成文**：① 新对话开场只报磁盘事实（AGENTS 路由 6 + operator-dialogue 规则 6）；⑥⑩ 口径。② 早已修，销账。
- **同批附带**：music-expert 环境变量通用名补齐 + 两检索器 SSRF URL 白名单守卫（Mimosa L3 拦截 4 高危后修复）；**"底座+插件"四标准定案**（产物握手/依赖全环境变量/合同自带接线说明书/缺能力结构化 blocked）。
- 回归 8 套全绿；逐条去向见 002 清单尾部《测后裁决执行台账》。

## 3. subtitle-expert 剥出立册（`b43eb12`，本地未 push 等口令）

- **判据兑现**（09-21 上午评估）：字幕=唯一够格的下一个专员——跨 G2→G5、归属已裂（style-contract 在 G4、校验器在 G3）、事故链最厚（㉖㊍⑧⑩㉛），⑧=出生证。
- **落册物** `skill/subtitle-expert/`（pipelineNode: support，花名册已登记）：布局校验器自 video-edit-plan 迁入（⑧ 档零行为改动）＋新增 `subtitle_probe_renderer.py` 能力档探测（autoWrap=true 无实测证据即拒、缺 ffmpeg 结构化 blocked）；style-contract 自 local-video-render 迁入；`layout-contract.md`（含接线说明书）与 `qa-contract.md` 立为唯一事实源。
- **三不建防过度设计**（出入如实记于章程《执行记录》）：断行/ASS-SRT 同源生成引擎、字形覆盖探测（㉛ 留 G4）、烧录哈希对账。
- **接线清扫**：G3 只调专员脚本、G4 指专员样式合同、G5 字幕面改指针；旧文件删除 grep 零残留。新套件 12/12，全量回归 9 套全绿。

## 3b. 同日复审"剥离就要彻底"（用户逐节点实测要求引发）

用户要求逐节点实测并质问"是不是都走专员、别节点一点专员一点"。归属复查抓到三处不彻底，当场改造：G4 内嵌的章节卡 cue 修剪（`trim_ass_cues`+ASS 时基解析）迁入专员 `subtitle_trim_cues.py`（G4 改 CLI 调用，只烧派生件）；G5 私有 SRT 正则删除，改产物握手 `subtitle-srt-check.json`（status+sha256 新鲜度双校验）；生成纪律入机验——`--srt` 从"计数+单调"升级为**逐 cue 对时对文**（10× 漂移必拒）、新增 `--source`（G2 口播句子 JSON 一块一排版块）、事件重叠判"强调拆层"。活体探测另抓真缺陷：libass 版本正则误读 configuration 行 flag（`--enable-libfreetype` 曾被当版本号），改为独立行才认、拿不到如实 unknown。逐节点实测全部用 002 实产物：G3 校验 13 events 全中、植入 10× 漂移被拒、trim 后批准源 git 零改动、G5 复检 passed、探测保守档落盘+无证据 autoWrap=true 被拒。套件 12→33，全量回归 9 套全绿。

## 4. 展示图与状态

- 总流程图 v0.2 定稿（补 确认G2 链、弧线标字修正）＋2x 高清 PNG 入库并更新 GitHub（`43421d5`，已 push）。
- 挂账不变：tiger《Infinity》换否、远端仓库改名、平台融入 sourceAsset 合同、gN 前缀改名缓行。Mimosa 门禁两次提示完整扫描结论未得（library_source 不可用）——不宣称项目安全，择日跑一次密封深扫。

## 5. 晚间发布收口

用户口令 push 后先打专名 tag `subtitle-expert`，随即改拍走数字版本系列：**发布 v1.4.0「字幕专员版本」**（VERSION=1.4.0、CHANGELOG 新节、annotated tag `v1.4.0`；专名 tag 撤销删除）。本版发布范围含 002 十四条裁决批次＋底座+插件定案＋subtitle-expert 剥出与彻底化（`b7cc9c2`…发布提交）。
