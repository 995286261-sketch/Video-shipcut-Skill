# 日报 2026-09-17（N8 测后裁决：26 条问题全部处置 + 验收准备）

## 1. 今日定性

毕业考（tiger-intro-001）遗留问题清单的**统一裁决与修复日**：34 条中的 26 条测后条目全部给出终态，4 批提交，全量回归 7 套测试全绿（HEAD `19d2b9d`）。修复过程本身又暴露一个值得记录的教训（§5）。

## 2. 四批修复

- **批 A `52c072c`（代码 10 条）**：①preference 正则吞行；④曲库 list 子命令；⑤**高危分发边界静默洞**（边界告警移出未清权分支，cleared 档案换项目也红牌）；⑧⑬回显/检索词自动版本不互踩；⑩充分性分母如实标注；⑪检索输出目录不再套娃；⑱㉔bgm-choice 强制"登记 JSON=音频字节+分析报告在档"配对。
- **批 B `27c6815`（校验器 4 条）**：㉕validate_g3_plan 批量报错（SoftFail 收集，一次全报，保留旧 payload 兼容）；㉗评审/审批模式对称（mappingMode 词表+时长一致性 review 期即生效）；㉚g4_prepare 对工作台项目强制 G4_ROOT；㉜chapters[].segments 归一化（dict 不再 TypeError）。
- **批 C `c6d5f09`（合同 8 条 + CLI）**：③pipeline_state 全子命令 `--unicode`；②跨项目视觉分析人工搬迁临时办法；㉓探索层=镜头检测优先；㉘styleRules 条条有着落否则门禁卡列未承接清单；㉙口令语义改写为可实现版本（真实回复含口令+全文 verbatim 存档，废"独立消息"君子协定）；㉜㉝新增《交付包组件 Schema》唯一事实源；⑫⑖复核确认测中已落条款。
- **批 D `19d2b9d`（渲染防线）**：㉛drawtext 字形覆盖预检——纯 stdlib 解析 cmap（格式 0/4/6/12 + ttcf 多 face），章节卡/标题栏/封面三条路线进 ffmpeg 前拦截"全 face 缺字"与"TTC 首-face 陷阱"，合同成文红线，合成字体夹具 6 项测试跨平台可跑。

## 3. 保留不改

⑥⑨⑭（曲库供给与模型稳定性事实）如实记录；㉞（mv 覆盖事故）转为**续接纪律**写入交接文档：动未提交文件前先核对，累积型文件只追加不整搬。

## 4. 回归

7 套测试目录全绿：material-pack-intake / music-expert / p0-c-pipeline / video-edit-plan(95) / local-video-render(30，含新增 6) / media-qa-delivery(14，含新增 1) / long-video-local-edit；`git diff --check` 干净。N8 清单尾部《测后裁决执行台账》逐条对应提交哈希。

## 5. 今日新踩的坑（如实）

㉕㉗ 重构首跑 38 红——根因是函数签名用了 `str | None`（PEP 604），系统 Python 3.9.6 在 import 期直接崩，全部用例死在同一个 traceback。教训：本仓运行时下限是 3.9，新代码用 `from __future__ import annotations`；已作为全局纪律看待（本轮其余新代码同样预防）。

## 6. 验收准备

`docs/验收-录制脚本-v0.1.md` 已就位：六幕 G0→G5 录制脚本（主演方案 A=小规模真跑 / 方案 B=tiger 展品讲解）、录前检查清单、建议拍进去的三组"能力边界"镜头、每幕门禁收据=后期剪辑锚点。用户录完后在脚本尾部回填记录。

## 7. 未 push 累计

本地 22 笔未推（`e9f55ed` 之后全部 + 今日 4 批）；口令到齐再 push origin。
