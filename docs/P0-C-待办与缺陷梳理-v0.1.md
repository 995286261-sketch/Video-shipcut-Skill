# P0-C 待办与缺陷梳理 v0.1（2026-09-09）

> 汇总 kshatriya-intro-002 第二跑暴露的 bug/缺口与全部未完成事项。基线历史问题以 `P0-C-基线实测问题总账-v0.1.md` 为唯一事实源，本文只收新增与在途，编号沿用总账（㉛ 之后为㊉–，本文另加 A 系列为工程缺口）。

## 〇、修复状态（2026-09-09 当日批次）

缺陷表 1（包装时点校验）、2（SRT 校验）、A1（图形层）、A2（py3.9 兼容）**已实现并实测**：g4_assemble 新增 `--chapter-cards`（卡期间自动裁开字幕 cue）与 `--title-bar`，测试 8/8；validate_g3_plan 增 `packagingDecisions` 时点/重叠/越界检查（负例实测拒绝 2:05 越界卡）；validate_g3_subtitle_layout 增 `--srt` 格式/单调/与 ASS 对齐检查；4 个脚本补 `from __future__ import annotations` 后系统 3.9 全部可导入。全量回归 19/19。待办② 完成；带卡重渲 002 候选需重过 G4 门禁，等用户指令。

## 一、Bug 与缺陷（本轮实测证据在手）

| # | 缺陷 | 证据 | 修复方向 | 优先级 |
|---|---|---|---|---|
| 1 | 发现㊊：G3 包装决定无机器校验 | 我写的章节卡时点 2:05/2:38 超出 103.32s 时间轴，进了批准卡才被 G4 装配暴露 | 章节卡/封面时点入 plan 字段，`validate_g3_plan.py` 加 `0 ≤ t ≤ timelineDurationMs` 检查 | 高 |
| 2 | 发现㊍：G3 SRT 生成换算 bug | ASS 百分秒（`,49`=490ms）被当毫秒直写，时间戳缩 10 倍，`g5_validate_delivery.py` 拒绝 | G3 产出 SRT 统一 `cs×10` + 零填充；字幕校验器加 SRT 格式检查 | 高 |
| 3 | A1：g4_assemble 无图形层（㉗ 实证） | 章节卡/标题栏两条 demo-quality-patch 规则仍"合同先行无实现"，002 被迫以选项 A 接受 | `g4_assemble.py` 增 `--chapter-cards`（句边界对齐、期间隐字幕）与 `--title-bar`，含测试 | 高 |
| 4 | A2：g5 脚本需 Python ≥3.10 未声明 | `g5_validate_delivery.py` 用 PEP604 无 future-import，系统 3.9 直接 TypeError | 全脚本统一加 `from __future__ import annotations` 或在 runtime-dependencies 硬性声明并预检 | 中 |
| 5 | 发现㊌：G2 旁白响度无链路 | CosyVoice 源 −23.9 LUFS/真峰 +0.5（PLR 24.4dB），G4 临场试 4 种链才到 −15.2 | **G4 侧已修（2026-09-09 晚）**：`g4_assemble.py --normalize-narration-lufs` 内置实测标准链 + ebur128 渲染后实测写装配记录（>1.5 LU 偏差记复核提示）。G2 配音清单登记目标响度字段仍并入接线批次 | 中→低 |
| 6 | A3：批准口令 UX 与纪律依赖 Agent | 用户全程从未打出带空格口令（确认G2/确认/确认G5），靠 Agent 归一化+逐字入档（发现⑧ 的延续） | **已修（2026-09-09 晚）**：`pipeline_state.py` 脚本级归一化（canonical/无空格/裸"确认"仅当节点、错节点号拒绝、含糊拒绝），state 存 canonical + Verbatim + normalizedFromVariant 留痕，下游校验零改动 | ✅ |
| 7 | A4：music-expert v0.2 修复滞留分支 | ebur128 −70 哨兵值误读修复在 feat/music-expert-001（2e7156c），main 未含 | 随分支合并进 main | 低（合并即消） |

## 二、设计缺口（非 bug，需决策）

1. **发现㊉：G3 无跨项目差异化输入**——证据驱动选段在同素材+同主体下必然收敛（002 与 001 源区间重叠 ~96%，用户已接受为长/短双版本）。方向：计划合同加 `avoidSourceRanges`（或 `siblingProjectRefs`），选段优先未用窗口，充分性不足再回落。
2. **④⑦：风格参考是否入 material-pack 哈希清单**——待用户拍板的登记策略（发现⑦ 遗留）。
3. **⑲ 落地复核**：身份判定自动闭环 + 充分性分流已在本轮实操验证（G3→G2 修正案闭环），但 SKILL.md 3.6 文本改写是否已完成待查。

## 三、待办（按优先级）

1. ~~**合分支**：`feat/music-expert-001` → `main`~~ ✅ 已合（cbec478；缺陷 7/A4 随之消失）。
2. **G4 图形层实现**（缺陷 3）：✅ 已实现（`--chapter-cards`/`--title-bar`，测试 8/8）。**尾巴**：用 002 计划重渲一版带章节卡成片作活体验收——需重开 G4 门禁，等用户指令。
3. ~~**G3 机器校验补强**（缺陷 1、2）~~ ✅ 已实现并负例实测（`packagingDecisions` 检查 + `--srt` 校验）。
4. ~~**G2 响度链路**（缺陷 5）G4 侧~~ ✅ 已实装为 `g4_assemble.py --normalize-narration-lufs`（标准链+渲染后实测入记录，比原"轻方案"更进一步）。剩 G2 配音清单 schema 加 `loudnessTarget` 字段 → 并入接线批次。
5. **music-expert 联网取证**：离线三链 + 对齐/标签/推荐 v0.3 全部实跑验证 ✅；**只剩真实检索**——等用户注册 `MUSIC_EXPERT_FREESOUND_TOKEN`（⑫ 收口）。接线按用户指示**先不动节点**，纯方案已出（`docs/music-expert-接线方案-v0.1.md`，§6 四项待拍板）。
6. **⑤⑦：G1 参考视频分析缓存脚本级强制**（复用 ㉛ cacheKey 模式）。
7. **ChatCut 分支从未实测**：本轮走 local_direct；阶段 B 合同（导入/时间线排布/字幕卡）仍无端到端证据，平台融入前需一次真实运行（用户评估欠账时定为第一优先候选）。
8. **低清升级路径**：640×360 源 → 需高分源回 G0 重登记（发现⑩ 遗留，1080p 交付的前置）。
9. **⑤ 瑕疵3**：G1 示例去具体化（低优文档清理）。
10. **4.5 参考实现产品化**：维持搁置，等输入素材与授权重新确认。
11. **远端仓库改名**（仍为旧名 Video-shipcut-Skill）：待用户决定。
12. **skill 目录 `g0-`–`g5-` 前缀改名**（2026-09-09 用户拍板**缓行**，"到时候统一再改"）：技术可行（改名+`name:`+路由表/文档引用，半日；测试深度不受影响）。挂到**平台整合命名合同定稿**一起做，同时一次性收敛散布各文档的 skill 名硬编码引用；历史产物 JSON 内嵌 `"skill"` 字段不重写，更名映射记入 CHANGELOG。
13. **参考视频音轨的人声分离预处理**（N3 实战暴露的噪声问题第四层）：解说混音轨上逐句 ducking 会污染能量曲线、人声会多计卡点。三层低成本缓解已就位——①执行免疫（驱动剪辑的数字只来自纯 BGM 文件）、②简报 energyShape 16 桶均值、③回显卡"混音事实"标注。根治需 Demucs 类分离（torch 栈+CPU 数分钟），**等 N2 找乐出现"要精确复刻参考片内乐曲"的真需求再评估**；用户定调：共用功能做成专员能力，节点只接线。

## 四、当前基线状态

- main = bd9bd8d（002 全项目 + music-expert 合并 + 可迁移性批次），origin 已同步；2026-09-09 晚起全部批次（align/标签/卡片固化/接线方案/花名册 + N1/N3 接线与 zaku-intro-001 真项目 G0）**未 commit**，工作区在手，等用户口令。
- 端到端案例：unicorn-001（demo）、kshatriya-001（155.4s）、kshatriya-002（103.3s）三跑完成，002 全程官方脚本链、零手工装配。
