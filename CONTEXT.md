# 本地自动成片

本项目定义多条本地原始视频经过可追溯切片和编排后生成成片时使用的统一语言，避免产品讨论、编辑计划和实现使用不同含义。

## Language

**原始视频（Source Asset）**:
用户提供且只读保存的一条本地视频，是所有切片和成片证据的源头。
_Avoid_: 原片副本、输入片段、素材段

**候选片段（Segment）**:
从原始视频时间线上识别出的候选区间，使用 `startMs/endMs` 表示；它是编辑决策，还不是导出的媒体文件。
_Avoid_: 切片文件、成片片段

**切片（Clip）**:
根据候选片段从原始视频导出的独立媒体文件，可追溯到唯一原始视频及其时间区间。
_Avoid_: Segment、原始视频

**成片（Final Render）**:
按照编辑计划对一个或多个候选片段进行排序、标准化和渲染后得到的最终视频。
_Avoid_: 合并文件、切片合集

**剪辑意图（Edit Prompt）**:
用户用自然语言描述成片主题、重点和期望效果的输入；它指导内容选择，但不替代可验证的输出参数。
_Avoid_: 渲染参数、平台配置

**风格配置（Style Profile）**:
定义画幅、节奏、字幕、转场、字体、封面和音频规则的一组可复用约束，用于稳定同类成片的视觉与听觉表现。
_Avoid_: 提示词风格、AI 自由发挥

**参考成片（Reference Video）**:
用户提供、用于提取叙事结构和风格特征的成品视频；它不作为待剪原始视频，也不授权复制其中的音乐、商标或素材。
_Avoid_: 原始视频、Style Profile、版权授权

**编辑计划（Edit Plan）**:
连接候选片段与渲染器的版本化机器可读计划；同时记录源素材时间线、成片时间线、字幕/覆盖层、音频和导出配置。
_Avoid_: 只有剪辑师能理解的文字脚本、最终视频文件

**证据引用（Evidence Ref）**:
指向源素材哈希、转写词句、检测结果或审核记录的结构化引用，用于解释某个片段或编辑决定的依据。
_Avoid_: 无来源的模型判断、普通文件路径

**质量报告（QA Report）**:
对渲染产物与编辑计划的可自动验证检查结果，包括黑帧、静音、重复、编码、尺寸和时长。
_Avoid_: 人工主观审美评分、渲染日志

**编排 Skill（Orchestration Skill）**:
可供 OpenClaw 或公司平台调用的 Agent 能力，负责解释输入、调用本地引擎、管理流程并返回结果。
_Avoid_: 剪辑引擎、单个 FFmpeg 脚本

**本地引擎（Local Engine）**:
可独立调用并以稳定合同完成媒体探测、切片、时间线渲染和质量检查的本地程序。
_Avoid_: Prompt、云端剪辑服务

**全链路本地（Fully Local）**:
原始视频、音频、转写、抽帧、判断输入和渲染产物默认都不离开运行机器。
_Avoid_: 仅原视频不上传
