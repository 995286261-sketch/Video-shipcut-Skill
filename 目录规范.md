# 目录规范

本规范约束 P0-C 项目的所有后续文件。目标是让交接、复现、审计和清理都不依赖聊天上下文。

## 根目录

根目录只保留项目入口与长期文档：

- `当前状态与决策索引.md`：继续项目的唯一快速入口。
- `PRD-v*.md`：已评审的产品需求文档。
- `CONTEXT.md`：术语表，不写实现细节。
- `项目总览.md`、`目录规范.md`：阶段和目录边界。
- `skill/long-video-local-edit/`：可分发的 Skill 包，内含 `SKILL.md`、`scripts/`、`config/`、`references/` 和 `tests/`。

## 工作台：现役项目节点产物

`工作台/<projectId>/` 是现役项目的正式节点产物根。新项目和明确续作不得把正式节点产物写入旧 `工作区/`。

```text
工作台/<projectId>/
├── G0-素材包/          # 素材登记、需求、授权、源媒体与 material-pack.json
├── G1-创作方向/        # 方向简报与参考视频分析
├── G2-证据与口播/      # 证据、转写、口播、事实决定与声音决定
├── G3-剪辑计划/        # 视觉索引、编辑计划、字幕、封面与人工审核记录
├── G4-剪辑与渲染/      # 可编辑工程、渲染记录与本地候选
└── G5-交付包/          # 唯一交付包与节点审计闭包
```

源媒体在 `G0-素材包/02_原始素材/` 中保持只读。`pipeline-state.json` 是唯一编排真相，所有活动项目都放在 `工作台/<projectId>/pipeline-state.json`；归档不得作为续作输入。

禁止在根目录存放：原视频、下载压缩包、模型缓存、临时图片、渲染产物、一次性脚本或未分类笔记。

## 旧工作区与候选目录

`归档/` 保存历史项目、验证物和外部资料，仅供审计与回滚；不得作为任何活动项目的输入。`新工作区/` 是更早的待验证提案目录，仍不启用，也不得写入正式产物。

```text
新工作区/项目/<projectId>/
├── 素材整理/
├── G1-创作方向/
├── G2-口播与事实/
├── G3-编辑计划/
├── G4-剪辑制作/
│   └── work/              # 可清理的切片、拼接表、缓存与临时渲染
└── G5-QA与交付/
```

- 后续若启用，节点归属、`work/` 清理边界与 G4/G5 的职责以本节为候选方案；启用前需要用户明确确认并更新项目索引。

## 历史工作区与验证目录

| 位置 | 允许内容 | 不允许内容 |
| --- | --- | --- |
| `外部资料/原始文件/` | 用户提供的原始素材、参考成片及登记信息 | 处理后的副本、生成产物 |
| `外部资料/候选项目/` | 未修改的开源候选原始副本、许可证和来源说明 | 对候选项目的直接业务修改 |
| `工作区/候选项目分析/` | 调研、静态审查、验证计划 | 原始仓库副本、正式 Skill 源码 |
| `工作区/合同草案/` | 尚未升格为正式规格的合同草案 | 运行产物 |
| `工作区/参考视频分析/` | 抽帧、联系表和参考分析结论 | 参考视频原件 |
| `工作区/fixture/` | 自有可公开复现的测试素材与清单 | 客户真实素材 |
| `工作区/验证/<task-id>/` | 单次验证的中间文件、渲染和日志 | 长期唯一资料 |
| `.scratch/` | 规划期决策地图及其决策票据 | 实现源码与运行产物 |

现役项目路径以 [项目布局合同](skill/p0-c-pipeline/references/project-layout-contract.md) 为唯一准则。历史 `工作区/` 与 `成品/` 产物只读保留；`工作区/验证/` 只用于旧 PoC、测试和失败复现。

## Pipeline State

For every active project, `工作台/<projectId>/pipeline-state.json` is the sole UTF-8 machine-readable orchestration state. It records the current node, valid artifact references, human gates, approvals, accepted warnings, authorization, and next action. Do not infer pipeline state from folder names, chat history, timestamps, archived materials, or legacy `run-manifest.json`.

`p0-c-pipeline` owns cross-node routing. Each specialist skill reads this state when present and records only its own node's artifacts and review points. G5 delivery location is `G5_BUNDLE_ROOT` from the project layout contract.

## 统一工具目录

`D:\WorkTool` 是本机共享工具与离线包目录，不属于本项目源码：

- 可放：FFmpeg、Python 运行时、模型、版本锁定离线包及其哈希。
- 不可放：项目的源素材、业务配置、编辑计划、客户信息或最终成片。
- 项目只记录工具版本、路径、哈希、许可证与安装/自检方法；不把二进制复制回仓库。

## 进入实现后的边界

- `SKILL.md`：面向 OpenClaw/公司平台的编排入口与使用合同。
- `scripts/`：可独立调用的本地引擎命令；禁止隐式下载和写回源视频。
- `config/`：Style Profile、Target Profile、默认策略；平台规格只在配置中出现。
- `tests/`：单元、合同、fixture 和失败样例测试。
- `docs/`：安装、离线发布、第三方声明、运维和故障排查。

每次新增文件前先判断归属；不能明确归属时先放入相应 `工作区/` 任务目录并登记，不在根目录临时落地。
