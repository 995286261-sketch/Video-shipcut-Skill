# P0-C 长视频切片与本地自动成片 Skill

状态：规划基线，待 fixture 验证与公司合同确认  
版本：v0.1  
日期：2026-08-13

## 1. 一句话定义

将用户提供的多段本地视频和一句剪辑意图，经过可追溯的本地分析与编辑流程，输出一条可播放的成片；同时保留机器可读编辑计划、片段来源和质量检查证据。

## 2. 背景与问题

当前业务需要把视频素材快速整理为社媒或其他平台可发布的成片。人工剪辑的主要摩擦不是“导出一个 MP4”，而是：

- 从多段素材中找出与主题相关的内容；
- 保留每个片段在原片中的准确时间码；
- 统一不同素材的画幅、帧率、编码和音频；
- 避免修改原片、幻觉字幕、虚假表达和版权不明音乐；
- 让同一批输入在干净目录中可以重复执行并得到可解释结果。

## 3. 用户与使用场景

### 目标用户

需要把已有视频素材快速加工成一条成片的内容运营、剪辑或项目执行人员。

### 核心场景

用户输入 2–10 段本地视频，并提供一句自然语言描述，例如“剪成一条 45 秒、节奏紧凑、突出产品使用过程的横版讲解视频”。系统自动分析素材、选择或组合片段、生成成片并返回证据。

### 非目标场景

- 在线下载视频或绕过平台权限；
- 自动判断用户是否拥有素材版权；
- 完全替代人工创意、品牌审核或最终发布审核；
- 第一版支持复杂动画、专业调色、复杂音频混音或任意格式文档直接变成视频。

## 4. 产品目标

### P0 必须达到

1. 多段本地视频能够生成一条实际可播放的成片。
2. 每个被使用的片段都能追溯到原片文件、哈希和 `startMs/endMs`。
3. 原始视频只读；新项目的 G0-G5 正式产物写入 `工作台/<projectId>/`，历史 `工作区/` 仅作兼容读取。
4. 输出规格通过配置传入，不把 Facebook、Instagram 或其他平台规格写死。
5. 生成机器可读编辑计划和 QA 报告，失败时给出具体阶段和原因。
6. 根据 `editPrompt` 提出可追溯的候选片段，人工确认后再生成成片。

### P1 目标

1. 提高候选片段的主题命中率、叙事完整性和跨素材去重质量。
2. 生成基于原始转写的字幕草稿、封面配置和元数据校验结果。
3. 在不修改合同的前提下扩展更多 Style Profile。

### P2 后置目标

- 品牌模板、自动封面设计、图片/文档转视频、复杂转场、音乐库接入和多平台批量导出。

## 5. 产品原则

- **本地优先**：媒体、转写和渲染默认不上传云端。
- **证据优先**：任何剪辑决策都能说明来自哪个素材和哪段时间码。
- **原片不可变**：只读源文件，不覆盖或重编码原片。
- **配置驱动**：平台、画幅、时长、字幕和音频策略均为配置。
- **可替换**：转写、场景检测、语义评分和渲染器可以独立替换。
- **人工可否决**：机器选择是候选结果，不能绕过内容和技术 Reviewer。

## 6. 用户输入

### 简化用户输入

```text
sourceVideos: [本地视频路径...]
editPrompt: 一句话描述想要的成片
targetProfile: 可选的平台、画幅、目标时长、字幕和封面配置
```

### 机器输入合同

沿用业务提供的 `media.edit.plan` 合同。业务要一次输入多段视频，而合同文字写为单数 `sourceAsset`；在公司平台确认前，本地引擎以多资产模型工作，平台适配层保留原字段并采用已确认的向后兼容扩展。

本地 fixture 的表达示例：

```json
{
  "schemaVersion": "0.1",
  "requestId": "...",
  "projectId": "...",
  "operation": "media.edit.plan",
  "sourceAssets": [
    {
      "assetId": "...",
      "sourceKind": "local-file",
      "sourceValue": "...",
      "authorizationRef": "...",
      "sha256": "..."
    }
  ],
  "editPrompt": "...",
  "transcriptRef": null,
  "targetProfiles": [
    {
      "platform": "generic",
      "aspectRatio": "16:9",
      "targetDurationSec": 45,
      "subtitle": {"enabled": true},
      "cover": {"enabled": false}
    }
  ],
  "brandConstraints": {},
  "musicPolicy": {"mode": "source-only"},
  "requestedAt": "..."
}
```

说明：用户界面可以只要求视频和一句话；缺省字段由项目配置补齐，但不能凭空补造授权、品牌规则或音乐许可。

## 7. 输出

### 用户主要看到

- 一条成片视频；
- 3–5 个可选切片（若候选不足，明确说明原因）；
- 简短的处理结果和需要人工确认的事项。

### 必须保留的机器产物

- 原片时间码清单；
- `segments`：片段来源、起止时间、标签、分数和理由；
- `editPlan`：排序、裁切、转场、字幕、封面、音频和导出参数；
- 字幕文件和封面配置（若启用）；
- `qaReport`：黑帧、静音、重复、编码、尺寸、时长；
- `metadataReport`：哈希、授权引用、依赖版本和配置校验；
- `evidenceRefs`、`warnings`、`humanReviewPoints`；
- README 和失败样例。

## 8. 子任务与依赖

```text
资产校验/探测
      |
      v
标准化工作副本 -----> 场景切点候选
      |                     |
      +-----> 本地转写 ----> 主题匹配与片段排序
                                  |
                                  v
                           editPlan / 时间线
                                  |
                                  v
                              FFmpeg 渲染
                                  |
                                  v
                              QA 与回执
```

### 子任务定义

1. **probe**：读取媒体时长、画幅、帧率、编码、音频轨和哈希。
2. **normalize**：只生成工作副本，统一拼接所需的技术参数。
3. **transcribe**：复用现有本地 `transcribe-local-video`，生成带时间码的机器转写。
4. **detect**：使用视觉切点和音频停顿生成候选边界。
5. **select**：根据 `editPrompt`、转写文本和候选边界打分，不直接修改字幕正文。
6. **plan**：生成稳定的 JSON 编辑计划，记录每一段的证据。
7. **render**：按照编辑计划导出切片和组装成片。
8. **qa**：对输出和计划做自动检查，标记需要人工复核的项目。

## 9. 开源工具策略

第一版优先验证以下组合，不在 PRD 阶段锁死版本：

| 环节 | 候选工具 | 结论 |
|---|---|---|
| 探测、裁切、拼接、编码 | FFmpeg/FFprobe | 首选底座 |
| 本地转写 | 现有 Faster-Whisper Skill | 直接复用 |
| 场景切点 | PySceneDetect | 候选能力，需 fixture 验证 |
| 机器时间线 | 自定义 JSON，必要时 OTIO | JSON 是对外合同 |
| 静音粗剪 | auto-editor | 可选，不负责主题理解 |
| 模板字幕/封面 | 后续评估 Remotion | 暂不进入 P0 |

所有第三方依赖在实现阶段必须核验当前版本、许可证、Windows 可用性和是否需要联网；不得仅凭项目名称形成合规结论。候选项目资料属于历史研究，不是当前默认依赖。

用户流程映射：`probe/normalize` 属于 G0/G4 内部子任务，`transcribe` 属于 G2，`detect/select/plan` 属于 G3，`render` 属于 G4，`qa` 属于 G5。任何子任务都不能绕过 G0、G1/G2/G3 人审门禁或 pipeline state。

## 10. 关键业务规则

- 不得把机器转写当作人工校对字幕；不确定文字必须标记。
- 不得生成原片没有依据的产品承诺、人物陈述或事实。
- 默认不加入版权来源不明的音乐；优先使用源音频或明确授权的音乐。
- 片段时间码使用毫秒，满足 `0 <= startMs < endMs <= sourceDurationMs`。
- 新项目输出目录必须位于 `工作台/<projectId>/` 下，且与源文件目录分离；若平台仍传入 `workspace_root`，它必须解析为该项目工作台根，不得解析到历史 `工作区/`。
- 失败不得静默降级为随机拼接；必须报告失败阶段和可操作建议。

## 11. 第一版验收标准

1. 使用自有 fixture 的 2–3 段视频，能够生成一条 MP4 成片。
2. 成片中每个片段均能在 `segments` 和 `editPlan` 中找到源文件哈希及原片起止时间。
3. 重跑相同输入和配置时，不修改源文件，并能复用已完成的中间步骤。
4. 改变画幅或目标时长配置后，输出参数随配置变化，而不是固定平台值。
5. 缺少音轨、输入不存在、路径越界、转写失败、编码失败时，返回明确失败状态。
6. QA 能检测并报告黑帧、持续静音、重复片段、尺寸、编码和最终时长。
7. 字幕正文只能来自输入字幕或机器转写，并在报告中标记机器草稿状态。
8. 在未提供音乐授权、品牌约束或 Reviewer 信息时，输出 warnings，不伪造已审核状态。

## 12. 失败与人工审核

### 自动失败

- 源文件不可读、无视频流或无法探测时长；
- 任何输出路径逃逸工作区；
- 时间码越界、重叠且无法机械修复；
- 渲染输出无法通过基本编码或完整性检查。

### 必须人工确认

- 主题匹配分数接近阈值；
- 机器转写包含人名、专业名词或无法识别文本；
- 品牌禁用表达、产品承诺或版权音乐规则未提供；
- 片段排序改变了原始叙事含义。

## 13. 版本边界

### v0.1 先验证

多段视频、主题提示词、原片时间码、基本语义选段、JSON 编辑计划、FFmpeg 成片和 QA。

### 暂不承诺

图片/文档自动转视频、自动生成完整广告文案、云端大模型剪辑、复杂动画模板、自动发布平台和版权审核结论。

## 14. 已决策的 MVP 规则

1. 第一版必须根据一句 `editPrompt` 自动提出主题相关候选片段；不得只按上传顺序拼接。用户或授权 Reviewer 在平台中显式确认候选，并将人审记录写入 pipeline state 后，才可渲染 Final Render。
2. 单次任务的目标时长以 `targetProfile` 为准；`default-horizontal-explainer-v1` 的默认 fixture 为 45 秒，MVP 验收范围为 30–60 秒。
3. 无音轨素材可作为画面候选，需在 `sourceProbe` 和 warnings 标记；不得伪造源音频或将其用作依赖口播的主段。
4. 字幕是“用户文本或本地转写草稿 + 人工审核”，不承诺自动交付可直接发布的字幕。
5. 验收优先级依次为：合同/证据与源片安全、可复现与失败语义、成片可播放和 QA、主题相关性、风格一致性、处理速度。

## 15. 剩余前置条件

- 2–3 段可用于内部验证的自有 fixture，以及每条关键片段的人工真值。
- 公司平台对单 `sourceAsset` 与多视频输入兼容表达的确认。
- 品牌资产、禁用表达、音乐规则和双 Reviewer 信息；缺失时必须产生 warnings，不伪造审核完成。
- Python 与本地转写依赖的受控安装和 Windows fixture 验证。
