# P0-C 项目交接文档

> 项目状态与交接记录，任何会话（含当前寰贸链工作区会话）读本文即可接手，无需回看聊天记录。最后更新：2026-09-09。

## 1. 项目是什么

P0-C（寰贸链项目代号，**原名 Video-shipcut**，v1.1.0 起统一更名）：长视频切片与本地自动成片 Skill。六个节点 G0–G5 的本地化、可追溯自动成片流水线，以 Agent Skill 形式组织。产品定位见 `PRD-v0.1.md`、`CONTEXT.md`（术语表）、`README.md`；Agent 执行规则见 `AGENTS.md`（处理任何视频类请求前必读）。

当前阶段目标：**把这套 Skill 融入公司平台**（OpenClaw/公司平台调用，见 CONTEXT.md 与 PRD §6 的 `media.edit.plan` 合同）。

## 2. 仓库现状（基线）

- 本目录即唯一主干：`/Users/chuanzhangbigye/工作/MySpace/Video-shipcut Skill`（文件夹名保留旧名，仓库内容已全部 P0-C 化；曾短暂迁至寰贸链后按用户要求还原）。
- `main`  HEAD 已与 origin 同步；代码基线为 `f4d0c60` = tag **v1.1.0**（合并 leader 交付补丁的提交；其后的提交均为交接文档自身的修订）。远端为 GitHub `995286261-sketch/Video-shipcut-Skill`，**远端仓库名仍是旧名，是否改名未决**。
- 历史 tag：`v0.1`（初始交付）、`v1.1.0`（合入 leader 补丁）。变更明细全部在 `CHANGELOG.md`。
- 合并自 leader 的 v1.1.0 交付快照已归档至 `/Users/chuanzhangbigye/工作/寰贸链/归档文件/P0-C｜长视频切片与本地自动成片 Skill v1.1.0（leader交付快照）`（与 v0.1 归档并列，2026-09-04 移入前经 `CHECKSUMS.sha256` 全量校验 95/95 通过），**只作交付记录，不在其上开发**。
- 示例项目 `工作台/unicorn-gundam-intro-001/` 是跑通 G0–G5 的真实案例（含 G4→G3 返工记录），是回归与验收参照。

## 3. 已完成（2026-09-04）

1. 交付版与 leader 补丁版逐文件对比，四类改动确认（重命名 / 工具链跨平台 / G4 质量补丁 / G3·G5 门禁传导），CHECKSUMS 校验一致。
2. v1.1.0 补丁合入主仓并统一 P0-C 命名（含根文档、README 重写、CHANGELOG 新建、流程图改 SVG）。
3. 合并后全量测试 12/12 通过，push 并打 tag；仓库位置最终确定为 MySpace 原路径。

## 4. 下一步（按优先级）

**最新进展与既定路线（2026-09-08，以下述顺序为准；下方历史条目保留作背景）：**

- 基线验证跑与问题总账已收口：30 项（29+新增㉛）中 **24 项已验证**，7 个修复批次全部落在分支 `fix/g0-template-contract-006`（功能提交 `ad5d7c4`→`5e291d3`，各批含独立回填提交；明细与测试证据以 `docs/P0-C-基线实测问题总账-v0.1.md` 为唯一事实源），程序性 bug 清零，全量回归 17/17 通过。
- **① 合分支**：`fix/g0-template-contract-006` 合回 `main`，固化 24 项已验证修复。
- **② BGM 分析独立 Skill**（用户 2026-09-08 决定："从这个BGM分析这个功能开始做……甚至可以单独封装成一个skill"）：**已完成 v0.1**——新建独立 `skill/music-expert/`（分支 `feat/music-expert-001`，提交 `efebb4e`），不塞六节点管线。覆盖 ⑯（`music_analyze.py` librosa+ebur128 确定性分析，含卡点表/能量分段/风格简报，已验证）与 ⑫ 的确定性前置（双轨找乐：Freesound 自动检索 + 人工许可登记，打分推荐）。**待办**：装常驻分析运行时 `P0C_MUSIC_RUNTIME_HOME`（WorkTool venv）+ 注册 `P0C_FREESOUND_TOKEN` 做真实联网取证；同事产品化"AI 找 BGM"运行证据仍未取得，故 ⑫ 记部分推进；G0/G3/G5 接线另起批次。
- **③ ⑤**：G1 参考视频分析复用加脚本级强制缓存（复用批次 6 ㉛ 的 cacheKey 模式）。
- **④ ⑦**：风格参考是否纳入 material-pack 哈希清单——待用户拍板的登记策略决策。
- **⑤ 瑕疵3**：G1 示例去具体化（独角兽/标题锚定），低优先级文档清理。⑩ 确认为合同预期行为，无需动作。
- 4.5 参考实现产品化（7 项拆分）维持搁置，等输入素材与授权重新确认；`render_4_5_original.py` 不改。

1. **与 leader 对齐补丁背景与 G4 缺口归属**：v1.1 质量规则目前是"合同先行"——封面合成、句子级旁白生成与真实音频对齐、固定底栏 ASS 字幕、BGM ducking、完整解码 QA 只存在于 `skill/local-video-render/references/demo-quality-patch.md`、`render-contract.md` v1.1 字段和 `examples/g4-render-profile.example.json`，`g4_prepare.py` / `g4_build_handoff.py` 等脚本**没有对应实现**（十条规则中仅"画幅策略"落进了 `g4_render.py`/`g4_validate.py`）。审计已确认（2026-09-07）：leader 口述的"人工门禁改自动审批""AI 自动找 BGM"等行为**不在交付快照内**（快照与主仓逐字节一致，CHECKSUMS 95/95 通过）。**已向 leader 核实闭环（2026-09-07）**：这两个行为她从未写进 Skill，是其在 TASK-050 中让 Codex 在运行时直接做的；交付包是忠实交付，无"包内门禁污染"需清理。处置结论：自动过审与 AI 找 BGM 属我方架构**明确不采纳**的运行时习惯（人工门禁保留、BGM 走登记制资产库）；她的 demo 中 9 条"合同先行"质量规则之所以效果达标，正是因为 Codex 在临场补位——该模式不可复现，平台融入（无人在场兜底）要求将其机械实现。
2. **端到端验证跑**：用真实素材完整跑一遍 G0–G5，记录门禁实际执行情况（哪些守住了、哪些被 Agent 自动推进）与 BGM 处理表现；重点观察 9 条"合同先行无实现"的规则在实操中 Agent 是如何临场执行的——这正是 leader 那边 Codex 补位的位置。观察结果并入梳理文档（拟放 `docs/`）。
   **卡片体系纸面审查已提前发现三处瑕疵（2026-09-07，决定带病跑基线、实测取证后统一修复，暂不动仓库）：**
   - 瑕疵 1（主要）：leader 的 v1.1 字段只进了 SKILL.md，没进固定卡片模板——G3 卡片缺 `aspectRatioPolicy`/句子级旁白边界/clean master/字幕时序来源字段，G5 卡片缺"章节卡不截断、无重复旁白/声道、许可记录"等检查项（G4 卡片基本无恙）。风险：人审批准对着的是旧清单。
   - 瑕疵 2：G0/G2 卡片的资料清单示例仍是旧路径 `工作区/素材包/<projectId>/…`，现行规范为 `工作台/<projectId>/G0-素材包/…`（G1 卡片示例已是新路径，佐证为迁移残留）。
   - 瑕疵 3（低）：G1 卡片回显示例过于具体（独角兽高达标题、"胡汉斯风格"），有锚定照抄风险。
   **G0 实测新增发现（kshatriya-intro-001，2026-09-07）：**
   - 发现⑥（高优先，真 bug）：G0 模板与 `material_pack.py` 机器字段不匹配——模板用"想做什么内容（一句话）/不能说或不能出现什么"+A/B/C 复选框，脚本要求 `想讲什么/给谁看/目标时长/输出/希望的感觉/不能说什么` 六标签 + `BGM decision: provided|use_library_later|no_bgm` 行。照模板填写 register 必失败（实测复现）。
   - 发现⑦（观察项）：`material-pack.json` 只对 02_原始素材/07_授权音频登记哈希，05_风格参考不入清单；跨项目分析缓存命中需手动比对 SHA-256（本次 kshatriya 项目实测：参考片哈希与 unicorn 项目 `reference-analysis-manifest` cacheKey 一致，缓存理论可命中但无脚本校验路径）。待决策：参考素材是否纳入哈希登记与 manifest 字段。
   **G1–G2 实测新增发现（2026-09-07）：**
   - 发现⑧：固定回显卡无机器强制——执行者（Agent）多任务时曾漏弹【G1 方向确认】回显被用户当场指出；"确认串"亦为语义级放行（"G1确认"有效、"确认G"拒绝）。harness 候选：回显必弹检查 + 批准串脚本校验。
   - 发现⑨（代码 bug）：`g1_direction.py write` 输出路径拼为 `工作台/创作方向/<projectId>/`，违反项目布局合同 `工作台/<projectId>/G1-创作方向/`；本次人工纠正。
   - 发现⑩：源片实测仅 640×360@23.98fps——demo-quality-patch 第 7 条低清预警正确触发并登记 warning；`preserve_source` 将输出 360p 候选，1080p 需回 G0 补高分源出新版本。
   - 发现⑪：macOS 系统语音缺包时 `say` 静默回退英文音色且**根本不读中文**（Eddy/Grandpa 试听经 ASR 回环验证只读出"666"）。harness 候选：配音试听应加 ASR 回读比对源文的机器自检；Tingting 实测可用。
   - 发现⑬（正面）：G2 转写缺依赖 → 结构化 blocked（不编造、不下载、不静默跳过）实测行为正确；转写 cacheKey 已留档可 reused。
   - 发现⑫（设计缺口，用户确认记录）：**BGM 音乐内容解析（节奏/BPM/段落/响度曲线）从未存在于原始设计**——PRD §57 明确将"音乐库接入"列为不做项，全文只有授权纪律；设计中的"节拍"是 G3.1 口播语义节拍（非音乐节拍）；leader demo 的"会挑音乐"系 Codex 临场。该能力是"AI 找 BGM 独立 Skill"组件的必需前置，属**新增设计**而非恢复丢失。
   **实测四个观察点**：① G3 回显是否显示 v1.1 新字段（不显示则记录卡片实际形态）；② G0/G2 清单回显用新路径还是旧路径；③ "好的/OK"等模糊回复是否会被误记为审批（必须显式"确认 G×"）；④ BGM 选 B（待补）时是否每节点被提醒、有无任何一步擅自配乐。测试后总结复盘，瑕疵 1/2 随修复批次统一处理（预计小 diff：卡片补字段行 + 示例路径全局替换）。
   **G3 实测新增发现（kshatriya-intro-001，2026-09-04，均入统一修复批次）**：
   - 发现⑭：`g3_align_narration_audio.py` 硬 import faster_whisper 且不读 `P0C_FASTER_WHISPER_HOME`，CLI 无 `--model-dir/--language`（模型目录只能靠 `HF_HUB_CACHE` 环境变量）；本次用 WorkTool venv + HF_HUB_CACHE 跑通，v0.1/v0.2 旁白 ASR 边界与合成时间码一致（±600ms）。
   - 发现⑮：macOS `say -o x.wav` 报 "fmt?"；须 `--data-format "LEI16@22050" -o x.caf` 再 ffmpeg 转 wav。G2/G3 句级旁白均走此 workaround。
   - 发现⑯（修正⑫表述）：BGM 节奏解析产物（onset/卡点表）在 unicorn demo 中存在但**无生成脚本**——概念在合同里、能力从未沉淀；合同要求 G3 登记 BGM 乐句/卡点，实测无法机器执行，本次以能量探针半成品（G3-BGM能量骨架-v0.1.json，未成文）替代后转阻塞。
   - 发现⑰：G2 口播时长估算（110–140s）与 Tingting 实测（188s 纯语音）偏差 ~40%；估算方法需改为按字符速率标定或强制"先合成后估"。
   - 发现⑱（契约缺字段）：`g3_extract_visual_analysis_keyframes.py` 读 `sourceEvidence[].relativePath`，但 evidence-contract.md 未要求该字段；G2 按合同产出的清单直接让脚本 fail（"registered source missing: None"）。
   - 发现⑲（用户裁决，需改合同）：SKILL.md 3.6/g3-plan.md"目标主体必须用户确认"与项目所有者裁决冲突——裁决为：身份判定由 Agent 自动闭环（加密抽帧自判），人工只审最终回显；素材不足走充分性分流（用户已预授权"缩短成品"分支）。修复批次：改写 3.6 为自动闭环+充分性分流门禁。
   - 发现⑳（账本设计缺口）：视觉观察账本 append-only 且 exact-key 唯一，**无改判/作废通道**——用户指认 vf-001 为驾驶员后只能以 promptVersion 升版（v0.2）+supersedesRecordId 变通；合同应定义 supersede 流程。
   - 发现㉑（G2↔G3 schema 漂移）：G2 审核决定按 g2 模板写 `status:"approved"`+嵌套对象，`validate_g3_plan.py` 要求顶层 `approved_for_g3/approvedNarrationRef/factCitationRef/voiceBriefRef/permittedFactIds/prohibitedTopics` 且 ref 必须是**存在的文件**（目录不行）；两节点 schema 未对齐，本次人工补字段。
   - 发现㉒（脚本静默失败）：`g3_extract_verification_frames.py` 在源末尾时间码（286811ms）处 ffmpeg 退出 0 但**不落盘**，脚本不检查输出存在即报 completed；需补存在性检查+边界回退。
   - 发现㉓（G0 授权音频未验真）：用户提供的 "Interlinked.mp3" 文件头 `CTENFDAM`（CD-DA TOC 扇区签名），非 MP3/Musepack，无任何解码路径——G0 register 只做哈希不探解码，损坏文件一路混到 G3 BGM 规划才暴露。修复：material_pack.register 对音频资产加 ffmpeg 解码探针，失败即 blocked。**根因（用户 2026-09-04 确认）：网易云加密缓存文件直接暴力改扩展名、未做格式转换**——重供后 register 必须先解码探针通过才登记，且授权说明应记"流媒体个人订阅来源，仅限内部测试、不得随片外发"。
   - 发现㉔（回显渲染规范缺失）：八列合同只规定 JSON 存精确毫秒，未规定**给人看的卡片必须用人类可读时间码**（mm:ss.mmm），首版回显输出裸毫秒被用户退回重排。
   - 发现㉕（g4_prepare 不读时长决定）：`g4_prepare.py` 不读计划 `durationDecision`，产出 `targetDurationMs:0` 与无意义的 `durationDeltaMs`；应以计划目标时长为准。
   - 发现㉖（G3 字幕 ASS 违反自身布局合同）：字号20下 7 条长句折三行，合同 maxLines=2 无人校验；G4 压平副本改字号14+语义断行修复（实测全部≤两行、最险一句三行仍在 70–84% 安全区）。修复方向：G3 生成 ASS 时按布局合同做行数/宽度机器校验。
   - 发现㉗（G4 最终合成无脚本入口）：脚本链只覆盖 prepare/render(裁切)/handoff/validate；concat、口播+BGM 侧链 ducking、字幕烧录、封面合成全靠 Agent 临场 ffmpeg——不可复现，正是§4 第3条"补 G4 脚本实现"的实证。
   - 观察点④实测：BGM 损坏后 G3 全程未擅自配乐、每步标注 blocked 待裁决——行为正确。
   **G3 结果**：短版口播 v0.2（169.835s，S13+S14 合并、S16 压缩）经充分性分流获用户批准；17 段全部起/中/终探针验证入账本（101 条）；`validate_g3_plan.py`（approved_for_g4）与 `validate_g3_callback.py`（final_review 17 行）双通过；状态机 G3 已批准，currentNode=G4。BGM 槽位保留待用户重供有效文件。
   - 发现㉘（人工 QA 抓到人机感配音，用户 G4 预览反馈）："配音听起来跟人机没有区别"——macOS 系统 TTS（婷婷）被耳朵一票否决，实证 demo-quality-patch "本地系统 TTS 仅适合预览"条款正确。但 G2 试听流程没有任何一步把"系统 TTS=预览级、正式需神经 TTS"显式告知用户，导致选声时按"能听"通过。修复方向：G2 声音卡必须标注本地系统 TTS 为预览级并给出神经 TTS/真人替代路径；候选升级路径=百炼 CosyVoice（`bl speech synthesize`）逐句生成→重建口播→G3 时间线随实测音频重排→G4 重渲。
   - 发现㉙（g4_prepare 固定输出文件名）：`g4_prepare.py` 恒写 `G4-可编辑工程-v0.2.json`，重跑直接覆盖上一批次清单（婷婷批清单被龙天批覆盖，仅切片得以归档）；应按输入计划版本派生清单版本或拒绝覆盖。
   - 发现㉚（g4_render 输出目录语义）：`--output-dir` 直接平铺段文件而非合同的 `clean-segments/` 子目录，两次运行传参不一致即产生布局漂移；应固定写入 clean-segments/。
   **G4 结果（候选待预览）**：17 段 clean slices 技术检查通过（640×360@24、无原声、总长误差仅帧取整）；clean master + 口播 A1 + BGM 整曲铺底（2s 淡入/侧链 ducking/尾部淡出，响度实测 -19.5/-18.4/-26.3dB）+ 固定底栏字幕烧录 + 封面合成图；候选成片 `final/kshatriya-intro-001-candidate-v0.1.mp4`（170.875s，FFmpeg 全片解码零错误）；pipeline-state G4=review_required，等用户预览确认后进 G5。
3. **补 G4 脚本实现**（平台融入的前置要求，leader 处已确认无需再问归属）：把 demo-quality-patch 的每条规则映射为可跑的机器检查，消除"靠 Agent/Codex 临场发挥"的不可复现环节。
   **G5 结果（已完成）**：用户确认”我确认了，效果蛮好了”，完整播放人工 QA 通过；G5 交付包 `工作台/kshatriya-intro-001/G5-交付包/交付包-v0.1/`，官方 `g5_validate_delivery.py --media` valid，状态机已进入 `completed`。警告已接受但未升级权限：640×360 低清、+68ms 帧取整、黑帧/静音/重复检测器未纳入运行时、内部测试不得外发/商用。问题总账见 `docs/P0-C-基线实测问题总账-v0.1.md`：29 项记录，其中 26 项进入统一修复/设计决策批次。

   **kshatriya-intro-002 端到端第二跑（2026-09-09，G0–G5 全闭环，`completed_with_accepted_warnings`）**：同素材同主体的”短版”（103.32s / 23 段，001 为 155.4s / 17 段），验证 Skill 可复现性。全程走 `g4_prepare→g4_render→g4_assemble→g5_build_delivery_manifest→g5_validate_delivery` 官方脚本链，未手工拼 ffmpeg；交付包 `工作台/kshatriya-intro-002/G5-交付包/交付包-v0.1/`，`g5_validate_delivery.py --media` valid（0 错误），黑帧/静音检测干净。四条已接受警告（未升级权限）：无章节卡/标题栏、旁白 −15.2 LUFS（目标 −14）、画面与 001 源区间重叠 ~96%、重复画面检测器未运行。**本跑暴露的新问题（并入统一修复批次）**：
   - 发现㊉（G3 卡片设计缺口，用户当场质疑）：G3 选段是”证据驱动”（每节拍挑最强画面证据），**无跨项目差异化输入**，同一源+同一主体必然收敛到同一批镜头（002 有 96% 画面与 001 重合）。用户裁决接受（长/短双版本定位），但合同应支持 `avoidSourceRanges` 类输入，让”同素材第二版”能主动选未用窗口。
   - 发现㊊（G3 包装决定无机器校验）：G3 卡片里我误把源片时码（2:05/2:38）当章节卡时点写入，超出 103.32s 成片时间轴——`validate_g3_plan.py` 不校验包装决定层的时点是否落在时间轴内。修复方向：章节卡/封面时点入 plan 字段并加 `0 ≤ t ≤ timelineDurationMs` 机器检查。
   - 发现㊋（g4_assemble 图形层能力缺失，实证㉗）：装配器支持旁白/BGM侧链ducking/ASS烧录/封面合成，但**不支持章节卡与第二文字层（标题栏）**；demo-quality-patch 的章节卡/固定标题规则仍停在合同层。修复方向：`g4_assemble.py` 增 `--chapter-cards`（句子边界对齐、期间隐藏字幕）与 `--title-bar` 图层。
   - 发现㊌（旁白响度归一受限）：CosyVoice longtian_v3 源积分 −23.9 LUFS / 真峰 +0.5 dBFS（PLR 24.4dB），线性与动态 loudnorm 均被真峰限制在 −16 左右；最终 `acompressor(8:1)+loudnorm` 链达 −15.2 LUFS。修复方向：G2 配音清单登记目标响度并内置压缩链，避免 G4 临场调参。
   - 发现㊍（SRT 时基换算 bug）：G3 的 ASS 用百分秒（`,49` = 490ms），我首版 SRT 直接 `int(cs)` 当毫秒致时间戳缩 10 倍，`g5_validate_delivery.py` 的 `SRT_TIME` 正则（要求 `HH:MM:SS,mmm`）拒绝。修复方向：G3 生成 SRT 时统一 `cs*10` 并零填充，加 SRT 格式机器校验。
4. **平台融入适配**：
   - PRD 遗留开放问题：业务 `media.edit.plan` 合同写单数 `sourceAsset`，实际多段视频输入，兼容表达待公司平台确认（PRD §6）。
   - 平台执行机环境基线：Python ≥3.10 + FFmpeg/FFprobe 必须在 PATH 或 `P0C_*` 变量声明（见 `skill/p0-c-pipeline/references/runtime-dependencies.md`、`toolchain-setup.md`）；Faster-Whisper 离线运行时与模型许可单独确认。
   - 交付包打包流程：快照目录里有 `CHECKSUMS.sha256` 惯例（交付产物，不进仓库）。

## 5. 本机开发环境（macOS arm64，跑测试用）

- 系统 `python3` 是 3.9.6（/usr/bin，SIP 保护不可删改），**跑不了本项目脚本**（PEP 604 语法直接 TypeError），也没有 pytest / brew。
- **默认 python3 已覆盖为 3.12（2026-09-04 用户指示）**：`~/.local/bin/python3` → uv 托管 CPython 3.12.14（`~/.local/share/uv/python/cpython-3.12-macos-aarch64-none/bin/python3.12`），并加了 `~/.local/bin/python` 同链接；新建 `~/.zshenv`（内容：`. "$HOME/.local/bin/env"`）使**非交互 shell/脚本**也走 3.12；交互终端原本就经 `.zshrc:13` 激活。验证：`zsh -c 'python3 --version'` → 3.12.14。系统 3.9.6 保留在 /usr/bin 不动，显式路径调用仍可用。
- 已装：`uv` 在 `~/.local/bin`（激活：`source $HOME/.local/bin/env`），Python 3.12.14 经 `uv python install 3.12`。
- **持久工具链（2026-09-07 用户指示安装，不在 /tmp）**：`/Users/chuanzhangbigye/工作/WorkTool/faster-whisper/runtime`（uv venv，faster-whisper 1.2.1）与 `…/WorkTool/faster-whisper-models`（Systran/faster-whisper-small 快照 `536b0662…`，经 hf-mirror 下载）。环境变量已写入 `~/.zshrc`：`P0C_FASTER_WHISPER_HOME=<runtime>/lib/python3.12/site-packages`、`P0C_FASTER_WHISPER_MODEL_HOME=<models>`。模型许可为 MIT 权重快照，外发交付仍需单独确认。
- **临时**测试环境（/tmp 重启即失，重建命令如下）：
  ```bash
  source $HOME/.local/bin/env
  uv venv --python 3.12 /tmp/p0c-venv
  uv pip install --python /tmp/p0c-venv/bin/python static-ffmpeg
  /tmp/p0c-venv/bin/python -c "from static_ffmpeg import run; print(run.get_or_fetch_platform_executables_else_raise())"
  mkdir -p /tmp/p0cbin && cd /tmp/p0cbin
  ln -sf /tmp/p0c-venv/lib/python3.12/site-packages/static_ffmpeg/bin/darwin_arm64/ffmpeg .
  ln -sf /tmp/p0c-venv/lib/python3.12/site-packages/static_ffmpeg/bin/darwin_arm64/ffprobe .
  xattr -dr com.apple.quarantine /tmp/p0c-venv/lib/python3.12/site-packages/static_ffmpeg/bin
  ```
- 跑测试：12 个文件都是 plain unittest，逐个跑（不需要 pytest）：
  ```bash
  PATH="/tmp/p0cbin:$PATH" for t in skill/*/tests/test_*.py; do /tmp/p0c-venv/bin/python "$t"; done
  ```
  （正式开发建议改用项目内固定 Python 或 brew 装 ffmpeg，别长期依赖 /tmp。）

## 6. 口径与红线（速记）

- 命名一律 P0-C / `p0-c-pipeline` / `P0C_*`；不要引入旧名。
- "自动成片"不跳过人工门禁；参考视频不得未经授权进成片；最终成片必须从 clean master 重新合成，禁止二次叠字。
- 仓库代码 AGPL-3.0-only；**媒体素材不适用**（README 有专门声明）；本仓库不附带客户素材/字体/音乐/模型。
- 源素材只读；产物只写 `工作台/<projectId>/`；`pipeline-state.json` 是唯一状态真相。
